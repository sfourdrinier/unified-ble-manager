<!-- docs/5.0.0-FIX-PLAN.md -->

> Identifiers are redacted: LAN addresses, hostnames, device addresses and
> local paths appear as `<placeholders>`. The maintainer keeps the unredacted
> original, together with every agent receipt and the environment runbook,
> outside this repository.

# 5.0.0 fix plan and physical-test ledger (PR #210)

The working ledger for the PR #210 review fixes: every finding, owner decision and physical result. Companion to [5.0.0-GATE-LEDGER.md](5.0.0-GATE-LEDGER.md). Scratch paths below refer to the orchestrating session and are historical.

**Status:** this is a historical remediation and evidence ledger, not a
release hold. The owner has authorized the `5.0.0-rc.0` release path. Current
publication status and the required `main`-tagged OIDC workflow are governed
by [`../RELEASE.md`](../RELEASE.md); this ledger must not be read as claiming
that npm publication has already occurred.

# PR #210 — Fix plan for all findings (PR210-01…34)

> **NON-NEGOTIABLE (owner, 2026-09-17): NO LEGACY, NO OPT-IN, NO FALLBACK. Everything must work, the new (Rust) way, on every host.** Every public factory on every host uses the Rust core, with no route back to legacy implementations. Legacy providers/addons/TS core/protocol-control route are deleted in Phase 4 immediately after the Rust path is verified end to end (global rule: never delete before verification); until then they are unreachable from public API and used only as parity references.
>
> **NON-NEGOTIABLE (owner, 2026-09-17): no regressions, no lost functionality, on any host.** Every capability, option, event and behavior the legacy/current implementation provides must work identically or better on the Rust path before release; a missing capability is a failing blocker test, never a reason to keep a legacy route. Every packet ships a legacy-vs-new capability parity test for its host. A gap is never closed by reporting `capability.unsupported` for something that used to work.

Base: `5.0.0` @ `427f5d9e`. Review: `~/Downloads/PR210-review-and-agent-work-order.md`.
Detailed specs (normative; read the one for your packet in full):

- `spec-tauri-desktop-core.md` — Tauri + shared desktop core (04–11, 13T, 22, 24, 25, 26)
- `spec-mobile.md` — mobile Rust owner (01, 09RN, 12, 13RN, 14, 15, 16, 17, 18-runtime)
- `spec-desktop-factories-packaging.md` — desktop factories + packaging (02, 03, 18, 19, 20, 21, 23)
  (all in `/private/tmp/claude-501/-Users-sfourdrinier-src-trackourhealth-unified-ble-manager/b87dcb50-2f7a-474f-9604-e6f1449df663/scratchpad/plan/`)

## Ground rules (all agents)

- Follow `AGENTS.md`: test-first (write the failing test, see it fail, then implement); never swallow a failure; no `as unknown`/`as any`/`as T`; documentation ships with behavior; generated artifacts are regenerated, never hand-edited.
- **No git commits, no pushes, no branch changes.** Work in the shared working tree at `~/src-trackourhealth/unified-ble-manager`. Touch only files your packet owns; if you must touch another packet's file, stop and report.
- Environment: `source /private/tmp/claude-501/-Users-sfourdrinier-src-trackourhealth-unified-ble-manager/b87dcb50-2f7a-474f-9604-e6f1449df663/scratchpad/env.sh` (rustup 1.98.1, Node 22, Homebrew Ruby). Use `rg`, `fd`.
- Tests that open the real Bluetooth radio abort with SIGABRT under this app (macOS TCC) — that is environmental; hardware tests must be `#[ignore]`/explicitly gated and are run by the owner from an authorized terminal.
- Never close a finding by weakening a test or relabelling synthetic evidence as physical.
- Do not delete legacy code until its replacement is verified end to end (global rule); keep it as an explicit non-default reference path.

## Orchestrator decisions (recorded; owner is informed and may overrule)

1. **PR210-24:** remove the post-timeout `is_connected` probe (AGENTS.md: "a result reads the operation's own answer"). A timed-out disconnect stays `operation.timed-out` with retained `Disconnecting` ownership; the later OS release surfaces as a lifecycle `Released` event and a retry returns already-released. Split `peer.list` failures from `PeerNotFound`.
2. **Liveness backstops** (apply only when the caller gives no budget): `LIVENESS_OP` 120 s, `LIVENESS_CLEANUP` 10 s, `LIVENESS_SCAN_START` 30 s, compensation 1 s. Caller budgets travel as relative `budgetMs`.
3. **Delivery mode (corrected by PARITY-INVENTORY.md):** each host keeps at least its legacy semantics and gains honesty. Android: honor the requested CCCD mode (legacy did). Tauri: restore 4.x's `require-notification` property check. CoreBluetooth (Node + iOS): legacy TS property check for `require-*` is preserved; delivery reported `unknown`. WinRT: legacy chose notify-then-indicate; Rust path must keep notify preference (btleplug prefers indicate → Windows OS adapter writes the CCCD itself) and may now honor `require-*`. BlueZ: legacy ignored mode; Rust path applies the property check (no app that worked before breaks: a requirement is only rejected where the characteristic lacks the property). Never report `indication:false` as fact.
4. **Mobile radio ownership — parity:** everything the legacy RN route supports must work on the Rust route before deletion of anything legacy: state restoration (iOS `CBCentralManager` restoration, Android background/foreground-service leases), multiple concurrent managers sharing the platform radio, background operation, bonding/pairing, MTU/PHY/priority requests, `listAdapters`. MOBILE-RUST must design the owner as a process-owned radio with per-session leases (not one radio per session) if that is what restoration and multi-manager require; the per-session-radio shortcut in spec-mobile.md is REJECTED where it loses any of these.
5. **Contract:** `NotificationValue.indication: boolean` → `delivery: 'notification'|'indication'|'unknown'` in the backend contract (5.0 is a major; CHANGELOG entry).
6. **NAPI distribution:** ship verified per-platform prebuilt Rust NAPI addons; no cwd / cross-platform fallbacks.
7. **NO capability regressions (owner directive 2026-09-17).** Every capability the legacy CoreBluetooth/WinRT/BlueZ backends report today (pairing/bonding/security state/unpair, adapter selection, MTU request/effective MTU/max write length, connected RSSI, adapter power/authorization, connection priority/PHY where supported, etc.) MUST work on the Rust path before that platform's default flips. Gaps are filled by narrow OS adapters inside `ubm-desktop` (Windows: `windows` crate WinRT APIs; macOS: CoreBluetooth via objc2) behind `RadioBoundary`, with btleplug for the common path — packet DESKTOP-PARITY. A parity test asserts the Rust backend reports and exercises every capability the legacy backend reports. Windows flip is gated on parity + physical proof on a Windows host.
8. **PR210-23 ADR status:** owner decision; not edited without the owner's quote.
9. **Correction to review:** PR210-06's claim that the Tauri TS side never sends `operation.cancel` is wrong — the shared IPC client does (`src/ipc/client.ts`, `src/electron/renderer.ts:195-200`); the cancel is lost on the Rust side. Rust also sets `retryable` on every aborted/timed-out error (`btleplug_dispatcher.rs:219-222`) — part of PR210-22.

10. **btleplug patching allowed:** where parity needs native objects btleplug owns (macOS `maximumWriteValueLength`/`canSendWriteWithoutResponse`, btleplug macOS `mtu()` stuck at 23, Windows CCCD mode/`MaintainConnection`, BlueZ `mtu().unwrap()` panic), vendor a minimally patched btleplug under `vendor/btleplug` via `[patch.crates-io]`, each patch documented + tested, license/SBOM regenerated. Prefer narrow OS adapters that don't need the patch when feasible.
11. **Parity source of truth:** `PARITY-INVENTORY.md` (per-host tables). Every row with a gap is a required fix owned by a packet below. **False-claim registrations** (capability registered but every call fails) are fixed first: the capability must actually work (preferred) — deregistering is allowed only where the legacy route also did not support it.
12. **(REVISED by owner override)** The legacy iOS reference path restored earlier is a TEST REFERENCE ONLY until Phase 4; `legacyTypeScriptCore` (both platforms) and every other legacy opt-in are REMOVED in Phase 4 along with the legacy code. The Apple/Android first-party TCK legs must be re-admitted against the Rust route (RN-PROVIDER).
13. **Defaults (REVISED):** all public factories use the Rust core now (Node macOS/Windows/Linux, Electron main, RN/Expo Android/iOS, Tauri). Parity gaps are FAILING blocker tests, never skipped; nothing ships until all are green. No legacy opt-in exists in public API.

## Phases and packet ownership

### Phase 1 (parallel)

| Agent           | Findings                                                                                                                                                                                                                                                           | Owns                                                                                                                                                                                                                                                 |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CORE**        | 04(core), 05(core), 06(core), 09(core), 11(core), 22(core errors), 24, 25, 26 + mobile-spec §4 "Agent R" `central.rs` additions (`open_with`/`CentralProfile`/observer `CentralSignal`, `peer_records`, `attachment`, `resource_counters`, btleplug cargo feature) | `crates/ubm-core/**`, `crates/ubm-desktop/**`, `bindings/napi/src/{dispatch,core_backend}.rs` (call-site updates + `stopScan(operationId)` only), `src/backends/bluez/bluez-rust-core-provider.ts` (stopScan id only)                                |
| **TAURI-TS**    | 06(TS budget), 22(TS)                                                                                                                                                                                                                                              | `src/tauri/transport.ts`, `src/ipc/manager.ts` (route remap only), `__tests__/Tauri*.test.js`                                                                                                                                                        |
| **IDENTITY**    | 18, 19                                                                                                                                                                                                                                                             | `scripts/release/*identity*`, `scripts/release/generate-build-fingerprint.js`, `bindings/*/build.rs`, `unified-ble-manager.podspec`, `ios/*.sh`, `android/build.gradle`, `android/*.sh`, `src/generated/native-build-identity.ts`, distribution docs |
| **MOBILE-WIRE** | 12 (TS codec)                                                                                                                                                                                                                                                      | new `src/backends/reactnative/rust-core-wire.ts` + tests (no other files)                                                                                                                                                                            |

### Phase 2 (after CORE lands its API)

| Agent                 | Findings                                                                                                                                                                                                                        | Owns                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **TAURI-DISPATCH**    | 04, 05, 06, 07, 08, 09, 10, 11, 13T, 22 (dispatcher side)                                                                                                                                                                       | `native/tauri/src/**`, `docs/TAURI.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **MOBILE-RUST**       | 01 (Rust half), 14 (Rust), 17 (native)                                                                                                                                                                                          | new `crates/ubm-mobile/**`, `bindings/jni/src/**`, `bindings/uniffi/src/**` + `.udl`, JNI `MobileCoreBridge.java`, golden vectors, `docs/MOBILE_RUST_WIRE.md`                                                                                                                                                                                                                                                                                                                              |
| **DESKTOP-FACTORIES** | 02, 03, 20, 28–31 + NAPI surface for parity: lifecycle take op, adapterState+watch, listAdapters/adapter id, readRssi, deliveryMode, scanner.plan + TS software name/manufacturer filters, scan share/join, cancellation op ids | `src/node-{corebluetooth,winrt,bluez}.ts`, `src/electron-main.ts`, `src/backends/desktop/**` (git mv of bluez provider), `src/desktop-core-addon.ts`, `native/desktop-core/**`, `native/load-node-api-addon.js`, `bindings/napi/**` (after CORE), `scripts/native-prebuilds/*`, `scripts/ci/{build-napi-addon,check-napi-artifact-packaging,napi-clean-tarball-acceptance}.js`, `package.json`, prebuild jobs in workflows, desktop tests, `docs/NODE.md`, `docs/ELECTRON.md`, API reports |

| **DESKTOP-PARITY** | 02 + all desktop rows of PARITY-INVENTORY §1–4 (macOS RSSI/max-write/long-write/WwR readiness/authorization/adv fields; Windows pairing/security events/MaintainConnection/CCCD mode/adapter select; BlueZ pairing+Agent1/ConnectDevice address targeting/adapter select/char flags/address type/access flags; Tauri RSSI false claim) | `crates/ubm-desktop/src/os/**` (new: windows/, macos/, linux/ narrow adapters), capability registration, parity test; coordinates with DESKTOP-FACTORIES on napi ops exposing the new capabilities |

### Phase 3 (after MOBILE-RUST freezes interfaces)

| Agent           | Findings                                                                                                                                                                                                                        | Owns                                                                                                                                                                                                  |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **ANDROID**     | 01, 14, 17 (Android) + PARITY §5 native: RSSI/MTU/priority/PHY, bonding, bonded peers, CCCD mode, scan platform options, all events, cancellation, FGS/companion interplay                                                      | `android/src/main/java/.../rustcore/**`, `radio/OwnedAndroidGattRadio.kt` (additive), `android/src/test/**`                                                                                           |
| **APPLE**       | 01, 14, 17 (Apple) + PARITY §6 native: RSSI, state restoration adoption into the core, events incl. authorization, multi-manager ownership                                                                                      | `ios/UnifiedBleRustCoreSessions.swift`, `ios/UnifiedBleRustCore.mm`, new `ios/UnifiedBleRustRadioAdapter.swift`, `ios/Owned/*` (additive), `native/protocol/tests/AppleRustRadioAdapterHarness.swift` |
| **RN-PROVIDER** | 09RN, 12, 13RN, 14 (JS), 15, 16, 17 (JS), 18 (RN runtime check), PARITY §5–6 JS side (platform scan options forwarded, peers/bonded, address targeting, security, false-claim registrations), restore legacy iOS reference path | `src/NativeUnifiedBleRustCore.ts`, `src/backends/reactnative/react-native-rust-core*.ts`, `src/backend-contract/gatt.ts` delivery change + ripple, jest tests                                         |

### EXIT CRITERION (owner, 2026-09-17): every legacy capability works on the new way

- Every row of PARITY-INVENTORY.md (all hosts) is implemented on the Rust path and proven by a test that exercises it through the ordinary public factory (plus physical evidence where the row is radio behavior).
- Before any legacy deletion, an independent audit agent re-derives the complete legacy capability list from `origin/main` and the legacy code (not from PARITY-INVENTORY) — every public option, capability registration, event, error path and behavior — and diffs it against the Rust path. Any row missing from PARITY-INVENTORY is added and implemented. Deletion proceeds per host only when that diff is empty.
- Legacy tests are ported to run against the Rust route (same assertions), not dropped.

### Phase 4 — integration, verification, legacy deletion

- After each host's Rust path is verified (tests + physical where required): delete its legacy code (TS core/protocol-control route, legacy providers, native/electron corebluetooth+winrt addons + prebuilds + workflows, BlueZ dbus-next backend + optional dependency, legacy RN native protocol dispatcher pieces not used as the OS radio layer, `legacyTypeScriptCore` + all legacy opt-ins, legacy-only tests replaced by Rust-route equivalents), regenerate API reports/SBOM/docs.
- Full gates: `pnpm install --frozen-lockfile && pnpm validate:evidence && pnpm test:package && pnpm test:plugin && pnpm lint && pnpm prepack && pnpm release:artifacts:check && pnpm docs:check`, `cargo test --workspace --no-fail-fast`, `cargo test --manifest-path native/tauri/Cargo.toml`, `cargo clippy -D warnings`, `cargo fmt --check`, `pnpm test:native-protocol:apple`, Android unit tests, iOS/Android example builds, `scripts/ci/preflight.sh --fast` (Linux host).
- PR210-21 comment corrections verified per comment; review doc + gate ledger + regression matrix updated.
- Independent review agents (one per packet, given code not conclusions) re-verify each of the 26 findings against its acceptance test.
- Physical: Polar H10 rows per host (owner wears the strap; Bluetooth-authorized terminal / devices).

## Finding coverage (every finding → owning agent)

01 MOBILE-RUST+ANDROID+APPLE+RN-PROVIDER · 02 DESKTOP-FACTORIES · 03 DESKTOP-FACTORIES · 04 CORE+TAURI-DISPATCH · 05 CORE+TAURI-DISPATCH · 06 CORE+TAURI-TS+TAURI-DISPATCH · 07 TAURI-DISPATCH · 08 TAURI-DISPATCH · 09 CORE+TAURI-DISPATCH+RN-PROVIDER · 10 TAURI-DISPATCH · 11 CORE+TAURI-DISPATCH · 12 MOBILE-WIRE+RN-PROVIDER · 13 TAURI-DISPATCH+RN-PROVIDER(+DESKTOP-FACTORIES for NAPI provider) · 14 MOBILE-RUST+ANDROID+APPLE+RN-PROVIDER · 15 RN-PROVIDER · 16 RN-PROVIDER · 17 MOBILE-RUST+ANDROID+APPLE+RN-PROVIDER · 18 IDENTITY(+RN-PROVIDER, DESKTOP-FACTORIES runtime checks) · 19 IDENTITY · 20 DESKTOP-FACTORIES · 21 each owner for its comments, verified in Phase 4 · 22 CORE+TAURI-TS+TAURI-DISPATCH (Electron audit → new follow-up) · 23 owner decision · 24 CORE · 25 CORE · 26 CORE

## New defects found during design (added as PR210-27…33; same rules apply)

| #   | Defect                                                                                                                                                                                                                                                                                            | Owner                                                                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 27  | Electron main-router turns a _finished_ write into aborted/timed-out `caller-decides` (`src/electron/main-router.ts:390-403, 1363-1377`); TS core derives retryability from error code (`src/core/errors.ts:154`) → write replay                                                                  | TAURI-TS (extended: `src/electron/main-router.ts`, `src/core/errors.ts`, their tests)             |
| 28  | ESM build keeps bare `require()` in legacy CoreBluetooth/WinRT loaders; failure masked as `capability.unavailable` (`lib/module/node-corebluetooth.js:75`)                                                                                                                                        | DESKTOP-FACTORIES                                                                                 |
| 29  | `createBluezBleManager` has no platform check; on macOS with a darwin addon it drives CoreBluetooth while claiming BlueZ (`src/node-bluez.ts:142-152`)                                                                                                                                            | DESKTOP-FACTORIES                                                                                 |
| 30  | Public scan filters never reach the Rust provider: manager sends empty `filter` + `plan`, Rust provider has no `scanner.plan` (`src/public/ble-manager.ts:1616-1656`, `bluez-rust-core-provider.ts:896-910`)                                                                                      | DESKTOP-FACTORIES                                                                                 |
| 31  | NAPI Rust provider reports `commitState:'confirmed'` for every write and ignores `deliveryMode` (`bluez-rust-core-provider.ts:1694-1697`, `operations.ts:158`) — desktop half of PR210-13                                                                                                         | DESKTOP-FACTORIES                                                                                 |
| 32  | Tauri `ensure_authority` can open two radios on a racing first call; central opened on Tauri's runtime instead of the shared desktop executor (`btleplug_dispatcher.rs:325-344`, `central.rs:558-560`)                                                                                            | TAURI-DISPATCH                                                                                    |
| 33  | Destroy treats `PeerNotFound` as already-released, and adapter listing failures are labelled `PeerNotFound` → adapter/D-Bus failure during destroy reads as clean release (`btleplug_backend.rs:219-223`, `btleplug_dispatcher.rs:2718, 2755-2761`)                                               | CORE (+TAURI-DISPATCH consumes split)                                                             |
| 34  | `disconnect` holds the core lock across `boundary.is_connected().await` (`central.rs:1191-1208`) — second PR210-04 cause                                                                                                                                                                          | CORE (removed with the probe, decision 1)                                                         |
| 35  | Public recovery advice tells callers to retry every aborted/timed-out error regardless of retryability (`src/backend-contract/recovery.ts:137-139`); public `BleError` doesn't expose retryability → a public caller can be told to replay a dispatched write                                     | FOLLOWUP-TS (Phase 2)                                                                             |
| 36  | Electron main compares the renderer's `performance.now()` deadline with main's `monotonicNow()` — possibly different clock origins (same class as PR210-06)                                                                                                                                       | FOLLOWUP-TS (Phase 2)                                                                             |
| 37  | A dispatched write that fails with a non-abort code (e.g. `operation.disconnected`) gets that code's generic advice (reconnect/retry-with-backoff) although it may have committed; needs commit state carried on the normalized error across IPC (protocol change; Tauri decoder uses exact keys) | FOLLOWUP-TS-2 (Phase 2, coordinate wire shape with TAURI-DISPATCH)                                |
| 38  | Core retries accumulate disconnect-failure records, so a later successful release still leaves the destroy record `ReleaseFailed` (untruthful cleanup record; PR210-09 class)                                                                                                                     | DESKTOP-PARITY (owns ubm-desktop after CORE)                                                      |
| 39  | BtleplugRadio refuses any `Some(DeliveryMode)` requirement → regression vs Tauri 4.x / legacy property-check acceptance; must answer from characteristic properties when unambiguous, platform-specific when both                                                                                 | DESKTOP-PARITY                                                                                    |
| 40  | Core `unsubscribe` after services-changed returns gatt.not-found and never disables the physical CCCD (OS subscription live until shutdown)                                                                                                                                                       | DESKTOP-PARITY                                                                                    |
| 41  | Core sets `commit` only on aborted/timed-out; dispatched writes failing with other codes report `commit: null` (core half of 37)                                                                                                                                                                  | DESKTOP-PARITY                                                                                    |
| 42  | `recoveryForError`/`BleError` should advise verify-state whenever `commit === 'uncertain'`, regardless of code (TS half of 37)                                                                                                                                                                    | FOLLOWUP-TS-2                                                                                     |
| 43  | Tauri attach path opens a second btleplug Manager (second CBCentralManager on macOS) for attachment identity + adapter.state; BtleplugRadio::open silently picks first adapter                                                                                                                    | DESKTOP-PARITY (core) then TAURI follow-up                                                        |
| 44  | `example-tauri/src-tauri/Cargo.lock` stale vs new ubm-desktop deps (CI `--locked`)                                                                                                                                                                                                                | Phase 4 integration                                                                               |
| 45  | CHANGELOG entries for Tauri wire additions (`commit`, subscribe `delivery`, lifecycle events, `budgetMs` validation)                                                                                                                                                                              | Phase 4 integration                                                                               |
| 46  | Mobile session never forwards a hard delivery requirement to the radio (Apple); decision C refusal must live in Rust                                                                                                                                                                              | MOBILE-RUST round 2                                                                               |
| 47  | Mobile PlatformFailure lacks adapter-unavailable / adapter-resetting / permission-restricted / permission-not-determined → legacy error identities lost                                                                                                                                           | MOBILE-RUST round 2 (+ANDROID/APPLE mapping)                                                      |
| 48  | Pre-send refused write reported commit `uncertain` instead of `not-dispatched` (no platform "not dispatched" answer on the wire)                                                                                                                                                                  | MOBILE-RUST round 2                                                                               |
| 49  | `ios/UnifiedBleRustCoreAdapterState.swift` dead; regression matrix lines 77–81 describe old router cases                                                                                                                                                                                          | Phase 4                                                                                           |
| 50  | Simulator smoke of module load + adapter.state not completed (needs `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` by owner for simulator tooling)                                                                                                                             | Owner + Phase 4                                                                                   |
| 51  | Packed package lacked `vendor/` (root Cargo `[patch]` → source-mode builds from the tarball, e.g. Expo `file:` installs, fail)                                                                                                                                                                    | FIXED by orchestrator: `vendor` + `!vendor/**/target` in package.json files (69 files, no target) |
| 52  | Restored peers consumed per manager in JS only; legacy consumed process-wide → second manager can re-adopt                                                                                                                                                                                        | MOBILE-FOLLOWUP                                                                                   |
| 53  | `counters.describe` is process-wide; legacy counters were per manager                                                                                                                                                                                                                             | MOBILE-FOLLOWUP                                                                                   |
| 54  | `preferredPhy` at connect refused (legacy ignored) — must be honored where the OS allows, honest elsewhere                                                                                                                                                                                        | MOBILE-FOLLOWUP                                                                                   |
| 55  | `scripts/ci/f01-factory-routing-proof.cjs` uses the old binding shape                                                                                                                                                                                                                             | MOBILE-FOLLOWUP                                                                                   |
| 56  | Malformed drain record now fails the session's delivery (`source-failed`) where legacy skipped it — accepted as correct (AGENTS.md: never silently swallow); documented behavior change                                                                                                           | RN-PROVIDER (done) — note for owner                                                               |

### From independent LEGACY-AUDIT-1.md (all must be fixed before any legacy deletion)

| #   | Gap                                                                                                                                                                                                                                                                                | Owner                                                   |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| 57  | Desktop adapter loss (power-off/reset/removal/bluetoothd restart) does no teardown: must cancel ops, end scans (`source-failed`) + subscriptions, release links with cause `'adapter'`, advance backend generation, emit reset → `backend-restarted` (CoreBluetooth, WinRT, BlueZ) | DESKTOP-PARITY-3 (core) + DESKTOP-FACTORIES-2 (TS/napi) |
| 58  | Desktop admission errors missing: `adapter.powered-off`, `adapter.resetting`, `adapter.unavailable`, `permission.denied/restricted/not-determined` before effects                                                                                                                  | DESKTOP-PARITY-3 + DESKTOP-FACTORIES-2                  |
| 59  | macOS first-usable-state wait (10 s) + `adapter-initialization-timed-out`                                                                                                                                                                                                          | DESKTOP-PARITY-3 + DESKTOP-FACTORIES-2                  |
| 60  | `AdapterPowerState` lacks resetting/unsupported; availability always `available`                                                                                                                                                                                                   | DESKTOP-PARITY-3 + DESKTOP-FACTORIES-2                  |
| 61  | btleplug merges repeated service/characteristic/descriptor UUIDs on all desktop OSes (occurrences lost)                                                                                                                                                                            | DESKTOP-PARITY-3 (vendored btleplug patch)              |
| 62  | WinRT discovery: cached services, silent cached fallback, swallowed errors → empty/dropped; stale cache after GattServicesChanged                                                                                                                                                  | DESKTOP-PARITY-3 (patch)                                |
| 63  | BlueZ scan `Transport: auto` (must be `le`) + `DuplicateData` always true; macOS `AllowDuplicates: YES` (legacy NO) — duplicates must follow the policy                                                                                                                            | DESKTOP-PARITY-3 (patch)                                |
| 64  | `duplicatePolicy: 'merged'` refused on desktop → breaks `scanForServices`/`scanUntil` defaults                                                                                                                                                                                     | DESKTOP-FACTORIES-2                                     |
| 65  | Connection-level maximum write length requires discovery (legacy did not)                                                                                                                                                                                                          | DESKTOP-PARITY-3 + DESKTOP-FACTORIES-2                  |
| 66  | Unsupported capability rows lost their limitation reasons (CB request-mtu/effective-mtu/phy; BlueZ pairing-generation privilege + blast-radius note required by AGENTS.md)                                                                                                         | DESKTOP-FACTORIES-2                                     |
| 67  | Backend ids / adapter ids changed (`unified-ble:corebluetooth`→`…-rust-core` etc.; adapter ids unstable) → restore legacy public ids                                                                                                                                               | DESKTOP-FACTORIES-2                                     |
| 68  | WinRT non-default adapter selection (legacy `FromIdAsync`) and `deployment` diagnostic                                                                                                                                                                                             | DESKTOP-PARITY-3 + DESKTOP-FACTORIES-2                  |
| 69  | RN Android advertisement `appearance` + `rawRecord` dropped                                                                                                                                                                                                                        | MOBILE-FOLLOWUP                                         |
| 70  | RN `diagnostics.traceMaximumRecords/Bytes` silently ignored; `traceDocument()` empty                                                                                                                                                                                               | MOBILE-FOLLOWUP                                         |
| 71  | RN `transferOwnership`/borrowed managers always `ownership.denied`                                                                                                                                                                                                                 | MOBILE-FOLLOWUP                                         |
| 72  | Expo `restoration.claim()` with Info.plist-only config now needs a JS option                                                                                                                                                                                                       | MOBILE-FOLLOWUP                                         |
| 73  | IPC `budgetMs`/`commit` wire semantics changed without protocol version bump (mixed versions fail as malformed, not incompatible)                                                                                                                                                  | FOLLOWUP-TS (resume)                                    |
| 74  | desktop provider header comment says legacy "stays reachable by explicit composition" (false)                                                                                                                                                                                      | DESKTOP-FACTORIES-2                                     |
| 75  | PARITY-INVENTORY.md is stale; must be regenerated before it gates deletion                                                                                                                                                                                                         | Phase 4 (second audit)                                  |

## Status log

- MOBILE-WIRE done: `src/backends/reactnative/rust-core-wire.ts` (+173 tests). Notes in COORDINATION.md.
- TAURI-TS done: 06 (TS budgetMs), 22 (TS), 27 (Electron + `src/backend-contract/errors.ts` `commitUncertainError`, `src/core/operation-coordinator.ts` dispatched-write → never/commit unknown; pre-dispatch commitState now `not-applicable`). CHANGELOG `[Unreleased] ### Fixed`. Files owned going forward by TAURI-TS/FOLLOWUP-TS: those plus `src/tauri/transport.ts`, `src/ipc/manager.ts`.
- ADR accepted (owner); `__tests__/Docs.consumer.test.js` lane-ADR expectation updated to Accepted + mapping section.
- Live RF on macOS blocked tonight: Claude app lacks Bluetooth TCC; Terminal.app automation prompt timed out (needs owner). Probe ready: scratchpad/live/scan-probe.cjs.
- FOLLOWUP-TS done: 35 (public `BleError.retryability`, `recoveryForError`, `verify-state` action; API reports regenerated), 36 (Electron relative `budgetMs` via shared `src/ipc/relative-budget.ts`, main admits on receipt). Docs: UNIFIED_SEMANTICS §13, WEB, CONNECTION_MANAGER. Pending: `docs/ELECTRON.md` budgetMs note (DESKTOP-FACTORIES).
- IDENTITY done: 18 (native-build-identity.js, shared `bindings/ubm_build_identity.rs` + build.rs in napi/jni/uniffi, fingerprint seals bindingSchema, Apple verify-rust-core.sh, Android JSON identity + publish gate), 19 (explicit modes both platforms, no .git inference, prepare_command removed, `native:*:prepare` scripts). OPEN: (a) Android jniLibs refresh after MOBILE-RUST exports `build_identity_json` (Phase 4: `android/refresh-prebuilt-jniLibs.sh`), (b) runtime identity check before BLE: RN-PROVIDER (RN) + DESKTOP-FACTORIES (NAPI; `build-napi-addon.js` must pass `--print-env napi` digests), (c) `etc/api/node-bluez.api.md` stale after CORE provider edit → regenerate (`node scripts/docs/check-api-reports.js --write`) by DESKTOP-FACTORIES, (d) anyone editing Rust re-runs `node scripts/release/native-build-identity.js --write`.
- RN-LEGACY-RESTORE done (decision 12): `createReactNativeAppleLegacyBackendProvider` restored from main (internal export), manager legacy flag routes Apple to it; Apple TCK registration re-pointed to the legacy provider and main's first-party TCK Apple leg restored (orchestrator). TCK + RN suites 421/421. PENDING docs/CHANGELOG note that `legacyTypeScriptCore` works on iOS again (RN-PROVIDER).
- CORE done: full API (see CORE interface in agent report, summarized in spec addendum below). Owner sign-off pending on decision 1 (probe removed) and 2 (backstops). Added read_rssi, adapter_state/adapter_events, CentralProfile.adapter_id, list_adapters, PARITY_GAPS.md updated.
- TAURI-DISPATCH done: 04–11, 13T, 22, 32, 37 (Tauri+Electron wire `commit`), Tauri RSSI restored; 75 tauri tests (baseline 43), clippy/fmt clean; docs/TAURI.md updated. require-notification production parity awaits finding 39 (DESKTOP-PARITY).
- FOLLOWUP-TS done: 42 (commit-uncertain → verify-state for any code; `BleError.commit`, `BleCommitUncertainty`; coordinator marks any dispatched write failure uncertain), 45 (CHANGELOG Tauri wire entries). Phase 4 must regenerate: SBOM (`pnpm release:artifacts`), native-build-identity (`--write`), API reports after DESKTOP-FACTORIES lands (electron-main/node-\* reports were generated from in-progress source).
- DESKTOP-PARITY round 1 done: 38, 39, 40, 41, 43 + OS adapters (Linux zbus pairing/Agent1/ConnectDevice/power/services-resolved/flags/MTU; Windows pairing/MaintainConnection/CCCD rewrite/adapter listing; macOS authorization + vendored btleplug maximumWriteValueLength). Type-check-only on Windows/Linux (physical pending). Round 2 running: macOS adv solicited/overflow/connectable, Windows adapter authorization, BlueZ session bus.
- IDENTITY hardening done: digest follows [patch] paths with cargo feature resolution; implicit build.rs + ubm-mobile covered. Phase 4: update docs/5.0.0-DISTRIBUTION_CONTRACT.md §4 sourceDigest definition.
- CORE (resumed by mobile-rust, then stopped): added PeerRecord/LifecycleEvent.database_generation, CentralResourceCounters.{queued,dispatched}\_operations.

## OWNER DECISIONS PENDING (morning)

E. Acknowledge defensible behavior changes (LEGACY-AUDIT-1 'Defensible changes' list): delivery vocabulary, write-without-response commit unknown, cancellation semantics, subscription end reason service-changed, Windows non-default adapters, adapter-off at creation, extra adv fields, malformed drain → source-failed, renderer absolute deadline rejected.
F. Removal of legacy boundary-injection exports/types (create*BackendProvider with boundaries, createNative*Boundary, DbusNextBluezBoundaryFactory) — consistent with no-legacy directive.
A. PR210-24: timed-out disconnect stays timed-out + retained ownership (probe removed).
B. Backstops 120 s / 10 s / 30 s / 1 s.
C. macOS: hard `require-indication` on a notify+indicate characteristic is refused (CoreBluetooth only enables notifications; legacy accepted and silently delivered notifications).
D. Connected RSSI on Windows/Linux: btleplug only has advertisement RSSI there; legacy WinRT/BlueZ had no connected RSSI, but Tauri 4.x returned btleplug's (advertisement-derived) value on all OSes → Rust path now reports unsupported on Tauri Windows/Linux. Options: honest unsupported vs "last observed RSSI" with explicit source semantics.

- DESKTOP-PARITY round 2 done: macOS adv solicited/overflow/connectable (btleplug patch 2), Windows adapter authorization (DeviceAccessInformation, matches legacy addon.cpp:229-249), BlueZ session bus (vendored bluez-async patch 3, no fallback to system). Tauri test literal fixed by orchestrator (75 pass). Physical checks listed in agent report: Linux dbus-run-session + python-dbusmock bluez5; Windows authorization revoke; macOS solicited/overflow peer.
- MOBILE-RUST done: crates/ubm-mobile (process-owned MobileHost + per-manager MobileSession leases, shared scan, restoration adoption, background/companion ops), JNI MobileCoreBridge + UniFFI MobileCoreHost/Session, docs/MOBILE_RUST_WIRE.md (frozen), golden vectors. 23/34/25 Rust tests, JVM roundtrip, 5 cross-target checks. Legacy RN dead-file list in agent report (Phase 4). Android jniLibs refresh pending.
- Phase 3 started: ANDROID, APPLE, RN-PROVIDER against the frozen TurboModule contract (COORDINATION.md).
- DESKTOP-FACTORIES done: 02, 03, 20, 28–31 + all 28 desktop parity rows implemented on NAPI/TS; no legacy route; darwin-arm64 prebuild; clean-tarball acceptance npm+pnpm with 6 negative legs; Electron smoke; CI L2 step fixed. Deletion list in agent report (Phase 4). Orchestrator updated ReleaseArtifactsRust pinned graph 173→201 (added: zbus stack + ubm-mobile; nothing removed).
- Env note for Phase 4 gates: PATH must put ~/.cargo/bin, Node 22 and Homebrew Ruby first (scratchpad/env.sh).
- APPLE done: UnifiedBleRustCore.mm + Sessions.swift pass-through + UnifiedBleRustRadioAdapter.swift; all PARITY §6 rows mapped with harness proofs; restoration identifier derived as legacy; XCFramework/bare/Expo iOS builds pass. Physical steps: scratchpad/plan/APPLE-PHYSICAL-TEST.md.
- MOBILE-RUST round 2 done: 46 (decision C single policy fn `unenforceable_requirement`), 47 (4 new failure kinds → legacy codes), 48 (`dispatched` flag → commit not-dispatched). APPLE resumed to adapt Swift to `Failed(.., dispatched)` + precise kinds; ANDROID told (MobileCorePort.kt nativeCompleteFailure 5-arg).
- ANDROID done: rustcore/ rewrite (process host, RadioHost adapter over OwnedAndroidGattRadio, all PARITY §5 rows, dispatched flag + new failure kinds), 260/260 JVM tests, assembleDebug OK, emulator loads module (blocked at JS openSession until RN-PROVIDER lands). Physical script: scratchpad/plan/ANDROID-PHYSICAL-TEST.md.
- RN-PROVIDER done: 01(JS), 09, 12–18 RN side, all PARITY §5–6 JS rows, Expo host services on Rust session, TCK legs on Rust route via wire-level double pinned to golden vectors; legacy opt-ins removed from public API; delivery ripple across all providers; 3013/3014 jest (the 1 later passes). Dead-file list in its report (Phase 4).
- FOLLOWUP-TS done: 73 (IPC protocol 3 on Electron + Tauri, v2 peers refused `protocol.incompatible` at bootstrap; channel name kept for typed refusal). Tauri cargo 76 pass.
- MOBILE-FOLLOWUP done: 52 (peers.claim-restored process-wide), 53 (per-session counters + process totals), 54 (Android PHY mask on connect; Apple refused as legacy), 55 (f01 proof rewritten), 69 (appearance/rawRecord), 70 (trace bounds/document), 71 (transfer semantics equal to legacy, proven side-by-side), 72 (Info.plist-only restoration). Android 265/265, Apple harness pass.
- 76 FIXED (FX5, Phase 4 cleanup): ReactNativeRustCoreProviderOptions.trace exposes internal CoreTraceRecorder class in the RN API report; narrow to a public interface. FIXED (FX5: etc/api/react-native.api.md:64 declares `trace?: CoreTraceSink`; CoreTraceSink is an exported interface in src/core/trace-recorder.ts:49, implemented by the internal CoreTraceRecorder — the report exposes no class).
- 77 FIXED by orchestrator: literal NUL byte in react-native-rust-core-provider.ts linkKey replaced by the unicode escape; RN suites 532/532.
- TAURI-FOLLOWUP done: 57 Tauri side (adapter-loss cause, source-failed streams/scans, backend.reset on stale attachment), 43 Tauri half (single central, no second Manager, core adapter selection rules), adapter.state resetting/unsupported/authorization. Tauri cargo 81 pass x3. OPEN for Phase 4: CHANGELOG entry for Tauri adapter-reset vocabulary; adapter.state cancel only reaches power read (minor); admission refusals carry commit null vs not-dispatched (sent to desktop-parity-3).
- DESKTOP-PARITY-3 done: 57–63, 65, 68 in core + vendored btleplug patches 6–9 (now mandatory via build.rs). Workspace 590 pass on macOS; napi 46; tauri 81. Round 4 running: 78 (btleplug silently drops Lagged notifications — must surface as loss), 79 (FakeRadio peers scriptable), CHANGELOG.

## OWNER DECISIONS PENDING (added)

G. macOS scan duplicates: legacy addon always AllowDuplicates NO (even for policy 'all'); Rust path maps all→YES, first/merged→NO (honors the requested policy; more traffic/power for 'all'). Strict legacy = one-line change.
H. macOS open with Bluetooth off: fails after 10 s (legacy behavior) vs report adapter off (audit alternative).

- DESKTOP-FACTORIES-2 done: 57–68, 74 on TS/NAPI; parity table all implemented, no blocked rows; legacy backend/adapter ids restored; full jest 3106/3106, lint/docs/prepack pass, clean-tarball npm+pnpm --negative PASS. WinRT emits no backend-restarted on reset (legacy never did) — parity-correct, supersedes finding 57 text for WinRT. N-API now passes radio admission/teardown policy (fixed silent-off defaults).
- PHASE 4 PRE-DELETION TODO: move legacy _\_BACKEND_ID / _\_IMPLEMENTATION_VERSION constants out of legacy modules before deleting them; after the LAST Rust edit: `node scripts/release/native-build-identity.js --write`, rebuild debug addon + darwin-arm64 release prebuild, repack, rerun clean-tarball acceptance.

## PHASE 4 GATE RUN #1 (macOS, 2026-09-18 ~02:30)

install, validate:evidence, identity --check, lint, docs:check, release:artifacts:check, cargo fmt, clippy -D warnings (workspace), cargo test --workspace, tauri test + clippy, test:package (248 suites / 3106 tests), test:plugin (45), Apple native-protocol (source mode), Android testDebugUnitTest (265/0 fresh run, source mode) — ALL PASS.
pack-install-smoke: FAILS locally by design — requires the full cross-platform prebuild set (linux/win32/darwin-x64 desktop-core + legacy electron prebuilds) that only the CI publish matrix produces. CI-only gate; must pass in CI on the PR.

### From LEGACY-AUDIT-2.md (N1–N10 → 80–89); all block deletion

- 80 (N1, CRITICAL) RN Android: every write fails capability.unavailable until an MTU is reported; legacy wrote at default MTU with OS long writes → MOBILE-FIX
- 81 (N2) WinRT (NAPI + Tauri-Windows) with-response writes > MTU-3 refused bytes.too-large; legacy/Tauri 4.x accepted (OS long write); same for Android with-response after N1 → CORE-FIX (+MOBILE-FIX for Android limit)
- 82 (N3) /testing desktop TCK registrations still legacy-only; no Rust desktop first-party TCK legs → DESKTOP-TS-FIX
- 83 (N4) core pre-checks characteristic properties before read/write (legacy let the OS answer) → restore legacy behavior (no pre-check for read/write; keep delivery-requirement check which legacy had) → CORE-FIX
- 84 (N5) desktop provider: lifecycle/scan-terminal broadcast lag only writes a diagnostic (lost link stays connected; scan streams never end) → reconcile on lag → DESKTOP-TS-FIX
- 85 (N6) desktop provider drops malformed notification values (continue) → end stream source-failed like RN → DESKTOP-TS-FIX
- 86 (N7) btleplug WinRT watcher forces Active + extended adv; legacy Passive → restore legacy default (policy-driven if public option exists) → CORE-FIX
- 87 (N8) Android FGS leases now released at session.dispose; legacy held them on the module until invalidate() → restore legacy lifetime → MOBILE-FIX
- 88 (N9) RN protocol-control public API removal → owner decision I (consistent with no-legacy)
- 89 (N10) BlueZ name-prefix Pattern no longer pushed to SetDiscoveryFilter (software-only) → push Pattern (vendor patch) → CORE-FIX; Electron main BlueZ provider lacks busKind/pairingGeneration → DESKTOP-TS-FIX
- 76 narrow RN trace type → MOBILE-FIX. 75 superseded by audits 1–2 (inventory retired as deletion gate; LEGACY-AUDIT-3 will gate).
- Deletion prerequisites (relocations) listed per host in LEGACY-AUDIT-2.md; src/core, src/manager, src/public, plugin, ios/Owned/\*, OwnedAndroidGattRadio, background/**, expo/**, native/load-node-api-addon.js MUST NOT be deleted.
- CORE-FIX done: 81 (WinRT/Linux with-response + descriptor writes to 512 via OS long write; WriteLimits::os_long_write), 83 (read/write property pre-check removed; subscribe still requires notify/indicate), 86 (btleplug patch 11 WinRT passive, no extended adv), 89 core (patch 12 BlueZ Pattern name_prefix; start_scan_matching). Workspace 605 pass.
- 90: Tauri maximum-write-length reports mtu-3 for all modes vs core accepting 512 with-response → TAURI-FOLLOWUP (resumed).
- 91: FakeRadio answers Paired for an already-bonded peer (real OS: AlreadyPaired) → Phase 4 cleanup (core).
- TAURI-FOLLOWUP: 90 done (per-mode max write length from core; 512/513 route test). Tauri cargo 82, jest Tauri 94. Note: Tauri capability list still marks gatt:maximum-write-length and gatt:long-write unsupported — identical to Tauri 4.x (parity kept); advertising them now that they work is an optional improvement → owner decision J. CHANGELOG entry for Tauri per-mode max + backend.reset + adapter-loss wording pending (Phase 4).
- MOBILE-FIX done: 80/81 Android (ReadWriteLimits per mode; with-response 512 via Android long write; without-response min(MTU-3,512) with default MTU 23; iOS per-mode maximumWriteValueLength), 87 (module-scoped FGS lease scope; invalidate releases), 76 (CoreTraceSink public interface). Android 269/0, Apple harness pass, ubm-mobile session 38.
- DESKTOP-TS-FIX done: 82 (Rust desktop TCK legs, /testing names kept, options changed → decision K), 84 (lag reconciliation), 85 (malformed → source-failed), 89 TS half (localNamePrefix; Electron BlueZ busKind/pairingGeneration) + 5 TCK-exposed regressions fixed (attach offer, per-caller events, already-owned connect on CB/WinRT, security watch initial state, dispatchedOperations). jest 3132, lint/docs/prepack/tarball pass.
- 92: possible flake in desktop-adapter-loss.test.js (1 failure in 3 full runs) → gate run #2 runs it 25x + 2 extra full jest runs.
- 93: GATT occurrence numbering (core per-UUID vs TCK per-parent assumption) → audit #3 to determine contract.

## OWNER DECISIONS (added)

I. RN protocol-control public API removed (getNativeUnifiedBleProtocolControl, control option) — consistent with no-legacy.
J. Tauri: advertise gatt:maximum-write-length / gatt:long-write now that they work (4.x listed them unsupported; parity kept by default).
K. /testing first-party desktop TCK registrations: same export names, new options ({now, binding?, nativePeerId?}) — breaking for TCK consumers.

## PHASE 4 GATE RUN #2 (macOS) — ALL PASS: install, evidence, identity, lint, docs, artifacts, fmt, clippy, cargo workspace, tauri+clippy, test:package + 2 more full jest runs (248 suites / 3132 tests each), plugin 45, Apple native (source), Android 265+ fresh. pack-install-smoke remains CI-only.

- 94 (was 92): REAL RACE confirmed (1/25, captured on run 29): adapter-state read settled `operation.reset` when an adapter reset races it; legacy always answered the current snapshot → CORE-FIX (resumed) incl. Tauri + desktop TS, deterministic barrier test + 100x loop.

### From LEGACY-AUDIT-3.md (X1–X7 → 95–101)

- 95 (X1, CRITICAL, all hosts): ubm-core default quotas (128 GATT paths, 32 subscriptions, 16 connections, 16 peers) truncate discovery silently (skipped stream.quota, discover succeeds with partial DB); mobile central is process-shared so limits are app-wide; Tauri TS drops `skipped`. Legacy had no limits. → CORE-FIX (after 94): no fixed caps below legacy behavior (size from database / platform limits), overflow never partial-success; + TS-FIX for Tauri skipped surfacing.
- 96 (X2): desktop discovery order sorted by UUID (btleplug BTreeSet) vs contract "discovery order" → restore discovery/handle order → CORE-FIX
- 97 (X3): BlueZ unknown MTU → assumes 23 → without-response writes >20 refused; legacy no length check → CORE-FIX
- 98 (X4): BlueZ subscribe property pre-check (legacy BlueZ called StartNotify without check) → per-OS legacy semantics → CORE-FIX
- 99 (X5): no public-path test for duplicate-UUID occurrences ≥1 through the addon; TCK uniqueness keys omit UUID (runner-public-scenarios.ts:1076-1100) → TS-FIX
- 100 (X6): production entrypoints export test hooks (radio:'synthetic', hostPlatform, loadBinding, firstStateTimeoutMs, openSynthetic, DESKTOP_RUST_CORE_PARITY) → owner decision L (recommend: move to /testing)
- 101 (X7): AGENTS.md still says BlueZ uses optional dbus-next; package.json lists dbus-next → deletion phase
- 91 FakeRadio already-paired: TCK fabricates the answer in the harness → must be fixed in core FakeRadio → CORE-FIX
- TCKS-FIX done: 99a (TCK keys incl. UUID; new base scenario gatt.duplicate-uuid-occurrences-route-exactly on 6 first-party legs + deterministic), 99b (9 real-addon duplicate-UUID tests), 95 TS (skipped removed end to end; typed discovery failures reach public API on desktop/RN/Tauri), 91 (no fabricated already-paired), DispatchRadio forwarding guard test. core-fix: 95 core (no quotas below ATT handle space; whole-snapshot validation), 96, 97, 98, 91 core; registration made linear (65535 attrs ~2.2 s).
- 102: public managers ignore backend `diagnostic-warning` events (unified-ble-core applyBackendEvent has no case) → data-loss facts reported only as diagnostics vanish → DIAG-FIX.
- 103: rust-parity jest suites look for a Linux addon on macOS (pass with UBM_NAPI_ADDON=darwin build) → verify vs earlier gate runs; fix resolution if regression.

## OWNER DECISIONS (added)

L. Test hooks exported from production entrypoints (radio:'synthetic', hostPlatform, loadBinding, firstStateTimeoutMs, openSynthetic, DESKTOP_RUST_CORE_PARITY) — recommend moving to /testing.
M. TCK breaking change for third-party backends: fixture worlds must now include duplicate-UUID attributes to pass the new base scenario.

- DIAG-FIX done: 102 (every diagnostic-warning → public diagnostic trace via recordBackendDiagnostic; data-loss/lifecycle facts converted to typed channels on desktop + RN; 39 new tests; jest 3272 package / 3322 full with UBM_NAPI_ADDON). 103 resolved as environmental (parity suites pass with UBM_NAPI_ADDON=darwin addon).
- 104: RN lost db-changed/stream-end/security/restored control records have no owner re-read verb (trace-only) → MOBILE-FIX. 105: reconnect under new generation invisible to reconciliation → MOBILE-FIX.
- CORE-FIX done: 94 (adapter reads not reset-cancellable; barrier tests; adapter-loss jest 100/100), 95 (protocol limits: 65535 paths/subs per DB, 3840 links, 65536 peers; whole-snapshot validation; linear registration), 96 (discovery order), 97 (BlueZ no-MTU writes answered by OS), 98 (BlueZ StartNotify without pre-check), 91 core (FakeRadio AlreadyPaired). test:package 256/3272; workspace/tauri/clippy/fmt/lint/docs pass.
- 106: core caps 8 live ops per owner across links (legacy 8 queued per connection) → CORE-FIX. 107: 8 consumers per subscription (legacy unlimited) → CORE-FIX. + full audit for any other cap below legacy.
- MOBILE-FIX done: 104/105 (`session.reconcile` owner snapshot; provider turns every lost control record into its typed transition) + fixed real bug: delivered db-changed never invalidated on the real owner (double hid it). jest 3284, Android 269, Apple pass.
- 108: ubm-mobile caps below legacy RN (ingress 256 vs legacy 512 per binding; OP_ID_MEMORY 256; scan members) → MOBILE-FIX (resumed).
- CORE-FIX done: 106 (per-owner op bound = kernel memory bound 65536; 3 links × 9 ops test), 107 (consumers/subscription 65536; 60-consumer test) + cap audit fixes: per-consumer subscription buffer → public stream max (65536/4 MiB), scan observations 65536, desktop notification ingress 65536/16 MiB, btleplug broadcasts 4096 (patch 13), lifecycle broadcasts 4096. jest 3284, workspace/tauri/clippy/fmt pass.
- MOBILE-FIX done: 108 (ingress 512 per class ≥ legacy shared 512; op-id memory 4096/session; memory-bounds table in MOBILE_RUST_WIRE.md).
- 109: cancel classification memory-bounded (legacy exact via dispatch epoch) → MOBILE-FIX (resumed): monotone per-session admission sequence on the wire.
- MOBILE-FIX done: 109 (exact cancel via per-session admission sequence).

## PHASE 4 GATE RUN #3 (macOS) — ALL 20 GATES PASS

install, evidence, identity, lint, docs, artifacts, fmt, clippy, cargo workspace, tauri + clippy, test:package (258/3288), full jest with addon (260/3338), plugin 45, Apple native (source), Android unit (source, fresh), JNI mobile roundtrip, desktop suites 40x no flake, example iOS build (simulator) SUCCEEDED, example Android assembleDebug (source) SUCCESSFUL. pack-install-smoke remains CI-only.

### From LEGACY-AUDIT-4.md (all block deletion; treated as regressions under owner directive)

- 110 (R1) Node/Electron macOS: btleplug routes a notification into a pending read (no read/notify guard); legacy addon refused read-while-notifying (413), concurrent read (414), subscribe-while-read-pending (415) → CORE-FIX (vendored patch / macOS adapter)
- 111 (R2) desktop TS provider closes subscriptions on lifecycle events before draining queued core values (dropped uncounted); polling adds latency → DESKTOP-TS-FIX: drain-before-close like Tauri; reduce latency (event-driven wake if feasible)
- 112 (B1) no-deadline connects (incl. when-available) now time out at 120 s; legacy waited indefinitely → CORE-FIX (core connect: no liveness backstop for when-available / legacy-indefinite connects) + MOBILE-FIX (session.rs when-available)
- 113 (B2) structured platform error detail lost (NSError code, HRESULT/gattStatus, D-Bus name, androidGattStatus; RN GATT code platform.failure vs gatt.\*-failed) → restore legacy per-host error identity + metadata: CORE-FIX (DesktopError platform detail), DESKTOP-TS-FIX (binding mapping), MOBILE-FIX (wire + Android/Apple adapters + TS)
- 114 (B3) Tauri cancelled-then-succeeded cleanup: legacy retried 8× with backoff 100 ms→5 s + tauri.quarantine.exhausted; now single attempt + orphan debt → TAURI-FOLLOWUP
- Deletion list corrections: KEEP GattOccurrenceResolver.kt (port its unit test from the legacy dispatcher test first); MOVE native/protocol/tests/{AppleRustRadioAdapterHarness,AppleCoreBluetoothReadNotifyProvenanceHarness,AppleCoreBluetoothScanParserHarness,AppleCoreBluetoothBorrowerOwnerHarness}.swift before deleting native/protocol/\*\* and keep the script minus ingress/execution legs; ADD to delete: JNI EchoBridge/GattBridge exports + bindings/jni/src/{gatt_queue,core_backend}.rs, rustcore/RustCoreSessionRouter.java. X6 (decision L) and X7 still open.
- TAURI-FOLLOWUP done: 114 (4.x automatic cleanup retry schedule 100ms→5s ×8, `tauri.quarantine.exhausted`; improvement: window release no longer cancels retries → no orphan after window close). Tauri cargo 88 ×3, jest 97.
- 115: existing Tauri orphan tests on real clock may race first retry → make deterministic + 20× loop (TAURI-FOLLOWUP).
- Phase 4 final docs pass: CHANGELOG entries for all Tauri changes (adapter reset vocabulary, per-mode max write, retry schedule).
- TAURI-FOLLOWUP: 115 done (orphan tests deterministic; Tauri suite 20/20). Tauri clippy currently fails on DesktopError size from core-fix's 113 (core-fix to box the field).
- MOBILE-FIX done: 112 mobile (no-budget connects wait indefinitely; paused-time tests), 113 mobile (legacy RN error identity restored end to end: platform.failure + {domain:'android', code, metadata.androidGattStatus}, connection.lost for status 19, Apple NSError domain/code; wire `platform` detail; golden vector). Android 269, Apple pass, RN/Expo jest pass.
- CORE-FIX done: 110 (patch 14 CB read/notify guard 411/413/414/415 + fixed upstream dropped CB callback errors / descriptor crash), 111 core (queued values delivered before invalidation terminal), 112 core (connect without budget waits for OS), 113 core (patch 15 typed PlatformDetail per OS, boxed). jest 3315; workspace/tauri/clippy all targets pass.
- 116: platform error detail not surfaced over Tauri IPC → TAURI-FOLLOWUP. 117: WinRT OS service-UUID AdvertisementFilter (legacy) vs btleplug software filter → CORE-FIX (patch 16).
- DESKTOP-TS-FIX done: 111 (drain-before-terminal + event-driven waker; notification p50 <1 ms), 113 desktop (ubm-napi-error/3 platform field → legacy identity per host). jest 3315; 39/40 loop (run-22 ESM loader failure during concurrent bob build → recheck).
- 118: security / write-readiness / scan-end still poll-driven (legacy callbacks) → CORE-FIX + DESKTOP-TS-FIX.
- CORE-FIX done: 117 (patch 16 WinRT OS service-UUID AdvertisementFilter restored as 4.x; note: some Windows drivers drop matching 128-bit adverts — legacy trade-off restored), 118 core (wakes for security/readiness/scan-end). DESKTOP-TS-FIX done: 118 wired + tested (6 tests); ESM loader flake proven environmental (20/20 isolated). jest 3321.
- 116 → TAURI-FOLLOWUP (re-sent; do it for consistency with Node desktop identity).
- TAURI-FOLLOWUP done: 116 (per-OS platform identity over Tauri IPC; strict decoder; 4.x fallback shape). Tauri cargo 89, jest 107.
- CHANGELOG: new entries broke prettier (HEAD was clean); prettier --write would re-nest lists → reverted; final docs pass must fix entries' Markdown structure by hand (finding 119).

## PHASE 4 GATE RUN #4 (macOS) — ALL 21 GATES PASS: package 259/3331, full jest w/ addon 261/3381, 40× desktop+RN+Tauri jest loop clean, 10× Tauri cargo loop clean, plugin, Apple, Android, JNI, clippy/fmt all, identity, artifacts, docs, lint, example iOS + Android builds.

### From LEGACY-AUDIT-5.md (S1–S5 → 120–124); all block deletion

- 120 (S1, CRITICAL) scan observations: only btleplug Discovered/Updated become advertisements; BlueZ never raises Updated → a known device invisible to later scans; CB Updated only for named peripherals → unnamed reported once; Tauri 4.x also re-read peripherals every 2 s → CORE-FIX (event-layer tests required; stand-in radio can't catch this)
- 121 (S2) advertisements queued even with no active scan; stale sightings attributed to new scan (incl. BlueZ cached-device stand-ins at radio open) → CORE-FIX (+ DESKTOP-TS-FIX labelling)
- 122 (S3) observations are btleplug's merged per-peripheral state, not each advertisement's payload; macOS Updated dispatched before data applied; properties() errors swallowed → CORE-FIX (patch)
- 123 (S4) pairing / cancel-pairing / companion chooser time out at 120 s without caller budget (legacy waited for OS/user) → CORE-FIX (desktop pair) + MOBILE-FIX (mobile security.\* + companion.associate; provider sends no budget)
- 124 (S5) unsubscribe errors drop platform detail; BlueZ non-D-Bus failures lack platform.failure/bluez-dbus identity; operation ids changed from legacy (direct-gatt._ / winrt._ / bluez.\*) without CHANGELOG → CORE-FIX (core) + DESKTOP-TS-FIX (restore legacy operation id strings per host)
- Deletion prerequisites (do now): port GattOccurrenceResolver test off the legacy dispatcher test; move 4 Apple harnesses out of native/protocol/tests + update script; audit-5 DELETE additions (JNI echo/gatt, uniffi EchoSession, Android echo/gatt tests) and MUST-KEEP addition (napi EchoSession used by tck rust-driver) → PREP-DELETE
- MOBILE-FIX done: 123 mobile (security.pair / cancel-pairing / companion.associate wait without backstop unless budget; `awaited()`). Android 273, Apple pass, RN/Expo 655.
- Decision B (extended): 120 s liveness backstop kept (when caller gives no budget) for discover, GATT verbs and connection controls (RSSI/MTU/priority/PHY) — legacy had no per-op timeout (hang forever); connect/pairing/companion now wait indefinitely as legacy. One-line switch per op if owner wants strict parity.
- PREP-DELETE done: GattOccurrenceResolverTest ported (legacy test kept); Apple Rust-route harnesses moved to ios/**tests** (excluded from package); shared ids/compat/BlueZ helpers relocated to src/backends/desktop/platform-identity.ts etc. with legacy re-exports; RN limits to react-native-protocol-limits.ts; NEW deletion guard **tests**/ShippedGraphExcludesLegacy.test.js (no package export reaches legacy) passes. Android 273, Apple pass.
- CORE-FIX done: 120 (patch 17: every sighting per OS; BlueZ known devices reported on scan start; Tauri 2 s cadence), 121 (observations only during scan, scan-attributed, fresh queue per scan), 122 (per-advertisement payload on macOS/Windows; BlueZ labelled device-state; read failures counted; Windows short service-data crash fixed), 123 desktop, 124 core. jest 3359; all Rust gates pass.
- 125: BlueZ Name/Alias-only property changes produce no sighting (bluez-async emits no event) → CORE-FIX (patch vendored bluez-async).
- CORE-FIX done: 125 (patch 18 bluez-async DevicePropertiesChanged summary event → one sighting per signal). Linux-only tests compile-checked only.

## LINUX HOST CHECKLIST (owner; required before release)

1. `cargo test --workspace --no-fail-fast` (runs Linux-only ubm-desktop tests: BlueZ model, device-state label, zbus pairing plumbing)
2. `cargo test --manifest-path vendor/bluez-async/Cargo.toml` and `cargo test --manifest-path vendor/btleplug/Cargo.toml` (patches 3, 12, 17, 18)
3. `bash bindings/uniffi/run_uniffi_roundtrip.sh` and `bash bindings/jni/run_jni_roundtrip.sh`; `pnpm test:parity`
4. `scripts/ci/preflight.sh --fast`
5. clean-tarball acceptance with `--probe radio` (npm + pnpm), `dbus-run-session` session-bus mock (python-dbusmock bluez5)
6. physical: scan (second scan sees known devices; name-only change reported), Polar H10 HR session, pairing/cancel/unpair, systemctl restart bluetooth → adapter reset, btmon LE-only + DuplicateData per policy

- DESKTOP-TS-FIX done: 124 TS (legacy operation ids per host restored; core op kept in platform.metadata.coreOperation), 121/122 TS (takeScanObservation with scanOperationId + ageMs; foreign observations refused; provenance labels). jest 3359; final 40× loop clean; one unreproduced run-24 BlueZ admission failure → gate run #5 loops 60× with failure capture.
- 126 FIXED by orchestrator: build-napi-addon.js installed the addon by copying over the staged file (macOS code-signature invalidation killed running processes) → installAddon() writes a sibling temp file and renames it (new inode); main() only when run directly; **tests**/BuildNapiAddonInstall.test.js (2 tests, red→green); real build verified.
- Gate run #5 + LEGACY-AUDIT-6 running.

### From LEGACY-AUDIT-6.md (→ 127–134); all block deletion

- 127 (C1, CRITICAL) Node macOS/Windows: reconnect after disconnect fails peer.not-found until rescan (btleplug drops peripheral on disconnect) → CORE-FIX (keep peripherals / re-resolve via retrievePeripheralsWithIdentifiers / FromBluetoothAddressAsync)
- 128 (N1) subscribe enables CCCD before opening the value stream → first values lost (Node macOS, Tauri all) → CORE-FIX
- 129 (N2) disconnect event races buffered notifications (random select; forwarders aborted; epoch bump drops queued values uncounted) → CORE-FIX
- 130 (N3) macOS: pending discovery/descriptor ops never failed on disconnect (btleplug), hang to budget/120 s → CORE-FIX
- 131 (N4) desktop ingress notification drops counted but never attributed/reported to the subscription → CORE-FIX + DESKTOP-TS-FIX
- 132 (A1) Android: in-flight ops at link loss fail platform.failure instead of connection.lost → MOBILE-FIX
- 133 (E1) Expo error codes changed (background leases, companion association, restoration claim) vs legacy → MOBILE-FIX (restore legacy codes)
- 134 (DOC1) README.md:64 still says install dbus-next → DESKTOP-TS-FIX

## PHASE 4 GATE RUN #5 (macOS; pre 127–134 fixes) — ALL PASS (btleplug gate mis-invoked by orchestrator; correct invocation `cargo test -p btleplug --lib` 28/28). Loops: desktop+tck 60× clean (no recurrence of run-24 BlueZ admission failure), RN/Expo/Tauri jest 20× clean, Tauri cargo 10× clean; example iOS + Android builds pass.

- MOBILE-FIX done: 132 (AndroidGattLinkLost typed failure → connection.lost with gatt status; queued ops never-sent; iOS pinned as legacy NSError platform.failure), 133 (legacy Expo error-code table restored end to end via nativeCode; companion on Apple → capability.unavailable as legacy). Android 277, RN/Expo 650, Apple pass.
- CORE-FIX done: 127 (patch 19: peripherals kept across disconnect; macOS retrievePeripheralsWithIdentifiers; Windows reopen by address), 128 (stream before enable), 129 (drain forwarders before disconnect event, 1 s cap, counted), 130 (macOS fail all attribute waiters on disconnect/power-off; Linux DisconnectedDuringServiceDiscovery; Windows relies on OS — UNTESTED), 131 core (ingress drops → subscription upstream loss per overflow policy). jest 3391; all Rust gates pass.

## HARDWARE CHECKLIST additions

- macOS + Windows: connect → disconnect → reconnect WITHOUT scan (127)
- macOS: disconnect during discovery and during descriptor read → immediate failure (130); Windows: same, verify OS answers pending GATT calls
- Windows/macOS: subscribe to a device that notifies immediately after CCCD enable → first value received (128)

## PHYSICAL EVIDENCE (DEVICE tier)

- 2026-09-18 macOS (Mac Studio, Node 22, public createCoreBluetoothBleManager → Rust core → vendored btleplug → CoreBluetooth): Polar H10 E997042F scan/connect/discover (6 services, 9 characteristics)/subscribe 0x2A37 → live decoded heart beats (owner confirmed). Earlier run against a neighbouring "Band-WU 0086" (not worn) proved peer-link-loss handling: stream terminal connection-lost, lifecycle lost/peer-link-loss, clean unsubscribe/destroy. Pending: 10-min sustained session + idle strap-removal on H10.

### From LEGACY-AUDIT-7.md (Muse CLI; → 135–141 groups); all block deletion

- 135 macOS desktop F1–F11 (readiness overflow policy/reprobe/buffering, pre-admission `.aborted` op id suffix, when-available op id, per-connection GATT serialization removed, RSSI capability limits, absent-state vocabulary, max-write-length empty id, stream quotas) → DESKTOP round after CI-FIX
- 136 Windows W-R1–W-R3 (descriptor without-response identity, unknown adapter id identity, never-observed peer identity) + W-R4 physical → DESKTOP round
- 137 Linux B-R1–B-R4 identities/values; B-R5 (AlreadyConnected tolerance MISSING), B-R6 (disconnect confirmation Connected=false 1 s MISSING) → CORE round
- 138 Tauri T-R1 (device-gone disconnect must be authoritative released like 4.x — the operation's own answer; resolves conflict with decision A), T-R2 (extra is_connected before disconnect) → CORE + TAURI round
- 139 Android AN-1, AN-2, AN-3, AN-5, AN-6 (rejection identities, PHY/MTU validation, pair-abort release) → MOBILE-FIX (now)
- 140 iOS I-1, I-2 adapter-level test gaps → MOBILE-FIX (now)
- 141 finding 127 lacks a btleplug-layer test (ResolvePeripheral/add_peripheral/reconnect-without-scan) → CORE round
- 2026-09-18 macOS H10 milestone run (public createCoreBluetoothBleManager, Polar H10 E997042F): 430 decoded HR notifications over ~434 s (bpm + RR, delivery=notification, droppedItems 0), maxGap 3466 ms (at strap unclip — H10 stops on skin-contact loss before link drop); owner removed strap → stream terminal connection-lost + lifecycle lost/peer-link-loss detected while idle; lifecycle stream ends throwing connection.lost exactly as origin/main publicConnectionTerminalError; unsubscribe/disconnect/destroy all released. Node macOS H10 row: PASS (DEVICE). Remaining rows: Tauri macOS, iOS, Android, Linux, Windows.
- MOBILE-FIX done: 139 (AN-1 capability.unsupported, AN-3 platform requestMtuFailed path, AN-5 pre-26 PHY unsupported, AN-2 domain aligned — audit claim wrong: legacy also refused empty, AN-6 no change — legacy never released either), 140 (I-1 adapter refusals asserted; I-2 productionRadioConfiguration extracted + harness). Android 278, jest 3393, Apple pass; fixed flaky cancel_targets_exactly_one_operation ordering.

## PHYSICAL-TEST FINDINGS 2026-09-18 (Samsung SM-A376U1 + iPhone 16 Pro Max + 2x Polar H10) → 142–160

Owner founding principle recorded in AGENTS.md: same behaviour on every platform unless capability genuinely differs; one public name per physical event; identical reconnect decisions; Android is the reference.

- 142 RN find() stream.overflow: batched drain emitted synchronously into 1-slot 'latest' stream → one record per JS task (react-native-rust-core-drain.ts). FIXED + tests.
- 143 Screen-off delivery stall from setTimeout(0) yield (RN timers pause with host) → native drain round-trip boundary, no JS timers. FIXED + tests; physically verified Android 60 s lock.
- 144 "desktop-_" host identity leaked to mobile (generations, errors, logs) → HostIdentity supplied by host; legacy identities restored on every host (RN, desktop, Tauri); operation correlations legacy `operation-{n}`. FIXED + tests; physically verified ("1", corebluetooth-_ ids).
- 145 Adapter off/on: manager never recovered; supervisor parked forever after 10 s readiness timeout; destroy after loss reported release-failed → OWNER DECISION B: managers survive adapter loss (departure from 4.x), supervisor re-waits, destroy released, IPC protocol 4 rebind for Tauri/Electron, terminal `source-failed` everywhere. FIXED + tests; physically verified Android BT off 15 s.
- 146 effectiveMtu() raw TypeError (unbound method in public layer; legacy had same bug). FIXED + tests; physically verified.
- 147 maximumWriteLength on RN Android/iOS (new 5.0 capability, owner) + platform-accurate limitation codes. DONE + tests; physically verified Android.
- 148 Read while notifying refused on Apple (1031) → OWNER: must work; readReceipt() with provenance read-response | read-or-notification; btleplug patch #14 reworked. DONE + tests; physical iPhone ECG pending.
- 149 Android GATT 133 / HCI 0x3E and equivalents on all backends → retryability caller-decides (owner), no internal retry; mobile wire failure envelope now carries retryability (was dropped). DONE + tests.
- 150 Link loss during discovery misreported platform.failure (Android Boolean discover; host.rs dropped status) → connection.lost; unrequested disconnect lifecycle cause lost. DONE + tests.
- 151 Supervisor stopped on link loss during configure → OWNER: back off and retry (connection.lost), wait for adapter (operation.reset); operation.disconnected (app's own release) stops. DONE + tests.
- 152 RN discovery:continuous-scan not registered → discovery.kind wrongly system-chooser; Web shared ids. DONE + tests.
- 153 electron/main lacked BlueZ compatibility export. DONE.
- 154 Finding 132 reversed: Apple pending-op-at-disconnect → connection.lost with NSError detail (owner principle). DONE + tests.
- 155 One-name-per-event vocabulary table (11 events x 6 backends) in src/backend-contract/event-vocabulary.ts, docs/UNIFIED_SEMANTICS.md, pinned by TS + Rust tests; cross-backend supervisor decision test (66 cases). DONE.
- 156 FIXED (FX5) Windows authentication/encryption refusal still gatt.read-failed/gatt.write-failed because btleplug WinRT ignores GattReadResult/GattWriteResult.ProtocolError (the ATT error byte IS available on Windows) → must map to platform.security like other hosts. FIXED (FX5: `is_security_answer` in crates/ubm-desktop/src/errors.rs maps WinRT `protocol-error` + attError 5/8/12/15 to `platform.security`, pinned by `an_authentication_or_encryption_refusal_is_platform_security`; the CHANGELOG one-name Windows row is still stale — see the FX5 receipt).
- 157 FIXED (FX5) Committed Android prebuilt jniLibs stale (missing nativeCompleteRead etc.) → regenerate before release. FIXED (FX5: regenerated; `pnpm native:status --only android` reports fresh, staged and current digests match).
- 158 Example/test-driver work (examples-shared, ubm-test-driver/1, Expo/Tauri/Electron/Web/Node hosts, HMR dispose + stopAll, per-host strap selection) — test infrastructure, not library. DONE, host tests pass; Tauri/Electron/Web/Node not yet run on hardware.
- 159 Diagnostics wording (RN boundary ubm-mobile-wire/1, limitation text) differs from legacy — kept (truthful) per owner "do it right"; needs CHANGELOG entry. FIXED (FX5: entry present at CHANGELOG.md:56-64, 'Departure from 4.x — the React Native diagnostics and adapter limitation wording').
- 160 STILL OPEN (checked 2026-09-21: deletion phase not yet run; nothing deleted). Missing: the deletion pass after host verification. Original: OPEN (deletion phase): dead legacy files RustCoreSessionRouter.java, RustCoreAdapterStateReader.java, UnifiedBleRustCoreAdapterState.swift (+ unreachable legacy addon.mm path).

## PHYSICAL EVIDENCE 2026-09-18 ~15:20 PDT — two phones in parallel, one strap each (DEVICE tier)

- Samsung SM-A376U1 → Polar H10 E997042F; iPhone 16 Pro Max → Polar H10 E9B93D29; same shared scenarios (examples-shared, ubm-test-driver/1).
- device-info: PASS both (battery 80 % / 100 %). ECG 130 Hz 20 s: PASS both (Android 2482 samples, iOS 2701; 0 gaps/drops/overflow) — iPhone ECG proves finding 148 (read while notifying) physically.
- Real strap-off link loss (>2 min): PASS both, identical vocabulary: lifecycle `connected -> lost (peer-link-loss)`, stream terminal `connection-lost`, detected ≤1.5 s, supervisor reconnected with no rescan when the strap returned, manager.destroy released.
- 161 FIXED (FX5, one-name principle): while the peer is absent, the supervisor's reconnect attempts fail as `connection.failed` (caller-decides) on Android but `operation.timed-out` (caller-decides) on iOS — same physical event ("peer did not answer the connection"), two names. Supervisor decisions identical (backoff), attempt counts differ with timing. Decide one name + document in the vocabulary table. FIXED (FX5: one name `connect-deadline-expired` → `connection.failed` caller-decides in src/backend-contract/event-vocabulary.ts:141-147; ConnectDeadlineVocabulary.test.js green).
- 162 FIXED (verified 2026-09-21: `noteReconnect` fills reconnectMs from supervisor-connected, lifecycle-connected, or first new-generation value whichever arrives first — examples-shared/driver/scenarios/link-loss.ts:162-179; ghost-outage guard in `openOutage`). Test: **tests**/driver/link-loss-outage.test.js 4/4 pass. Original: OPEN (harness): Android outage record missing reconnectMs/firstValueAfterReconnectMs after a real loss (iOS recorded 123293 ms / 149 ms).
- Earlier today (Android only): screen lock 60 s PASS, Bluetooth off 15 s/on PASS, MTU/max write length PASS, scan PASS. iPhone screen lock and BT off/on still pending (needs iPhone Mirroring).

## 163 — RESTORATION (owner-mandated 5.0 feature; MUST ship; owner 2026-09-18)

Principle: restoration only ever reconnects DIRECTLY to devices this app already connected to (no background scanning for unknown devices; no PendingIntent scan fallback). Same app code, same public events/names on iOS and Android.

- iOS: CoreBluetooth state restoration (restoreIdentifierKey via Expo plugin option) + pending connect held by the OS for known peers; system-terminated app relaunched on BLE event; library replays restored connections (legacy restoration-connection-{n} identities, already pinned) through the public "restored" surface.
- Android: known peer reconnect via connectGatt autoConnect (`intent: 'when-available'`, physically verified 2026-09-18) + CompanionDeviceManager device presence (Android 12+, startObservingDevicePresence) to wake a killed process when a known associated device returns; library reconnects directly and emits the SAME public restored event/vocabulary as iOS. Builds on the existing associateCompanion capability. Platforms/versions without CDM presence report capability.unsupported with a reason.
- Deliverables: design doc (docs/BACKGROUND.md restoration section + ADR note), public API/vocabulary rows (event-vocabulary.ts), test-first unit/native tests, example scenario `restoration` in examples-shared (both phones), CHANGELOG.
- Physical tests (both phones, straps): iOS — build example with restore identifier, kill app from Mac (devicectl terminate, NOT user force-quit which disables restoration), strap sends → app relaunched, connection + subscription restored. Android — kill app process, strap out of range and back → app woken via CDM presence, reconnected, same events.
- Status: STILL OPEN (checked 2026-09-21: cold-start restoration pieces landed — X-R4/W2 code, Y5 hardware-qualified, presence arming 222-229/232/236 — but 234's background-resume design slice BG1 is running and docs/BACKGROUND.md has no resume answer yet). Missing: BG1 decision + implementation + both-phone physical proof. Original: OPEN — start after the current fix round lands and gates are green.

## ALSO QUEUED (owner 2026-09-18)

- 164 Tauri full physical run with the shared scenarios (one strap) side by side with a phone (other strap); then Electron and Node desktop the same way.
- 165 Second Android phone: rerun the full Android list (second OEM/version).
- 166 Real Google TV (remote = Google Home app; adb over network) and Apple TV (remote = iPhone Control Center; pair with Xcode): install + shared scenarios; Android TV emulator as smoke.
- 167 Wear OS support (Samsung watch): estimate ~1–2 days after 5.0 — Wear test host, permissions, background limits, docs/evidence.
- 168 ESP32-C6 controllable test peripheral firmware (dongle backed up in ~/Documents/Hardware Backups) — closes the "stand-in radio" test gap flagged by final review C.
- 169 iPhone physical 2026-09-18: screen lock 131 s PASS (132 values, max gap 1.9 s, bluetooth-central background mode, NOT restoration); Settings Bluetooth off 36 s/on PASS (adapter-loss → source-failed, waited on adapter.powered-off, reconnected + resubscribed 6 s after BT on).

GitHub issues (2026-09-18): 163 restoration → #212; 164 Tauri/Electron/Node → #213; 165+166 second Android + TVs → #214; 167 Wear OS → #215; 168 ESP32 peripheral → #216.

RELEASE SPLIT (owner 2026-09-18): 5.0.0 = all fix-plan findings + #212 restoration + #213 desktop physical runs (incl. Linux, Windows) + final review + legacy deletion. 5.0.1 = #214 second Android + TVs, #215 Wear OS, #216 ESP32 peripheral (unless quick bugs force an earlier 5.0.1). GitHub milestones 5.0.0 / 5.0.1 assigned.

- 170 FIXED (FX5): Tauri physical (macOS, example-tauri, 2026-09-18): every FILTERED scan fails protocol.malformed tauri.scan-query "normalized scan query digest is invalid" (TS vs Rust canonical digest mismatch for name/services clauses); unfiltered scan OK. Muse fix round running (muse-fix-tauri-digest.md). Blocks every Tauri scenario that finds a peer. FIXED (FX5: canonical digest check in native/tauri/src/scan_plan.rs:105-107; see the line-365 FIXED entry for the 21-shape corpus test).
- 171 FIXED (verified 2026-09-21: the test now drains both connects through the unarmed wrapper via `settleCoreEvents()` before arming the lag, so the armed lag deterministically misses only the staged release — **tests**/backends/desktop/desktop-event-reconcile.test.js:213-234). Test: full file 35/35 pass; the single flaky test 3/3 in isolation. Original flake (1 timeout under full-suite load on Linux) not re-observed here; Linux full-suite re-run still the final proof. Original: OPEN (flake): Linux full-suite run of 1a312b14 — "a lag that missed only another peer's release fabricates no transition on a live link" timed out once under load; passes 5/5 in isolation.
- Linux gates @1a312b14 (<linux-build-host>, Ubuntu 24.04, BlueZ 5.72): fmt, clippy, cargo 704/704 (Linux subscribe regression FIXED), btleplug, Tauri, N-API build, lint, evidence PASS; test:package 3609/3610 (171).
- 170 FIXED (Tauri digest: Rust canonical JSON now byte-identical to TS; IPC also carried no address filters — fixed; 21-shape shared corpus test).
- 172 tools/h10-sim Polar H10 simulator (test tool, not shipped) — Muse building (muse-h10-sim.md).
- 173 Brainstorm UBM peripheral/GATT-server role → GitHub #217 (design first, no code; owner next direction after 5.0).
- 174 h10-sim full Linux fidelity + remote control (owner): manufacturer data, known identity (name/serial/public address 90:DE:80:3B:69:78), real drop-link, H10-like pairing, systemd --user, joins test driver as host kind "peripheral-sim", authenticated remote API (token, loopback default), combined DUT+sim sequences, CI build/test; first-class repo tool, NOT in npm package. Queued after the Linux advertising fix (muse-h10-sim-full.md).
  - owner: location tool/h10-sim (under /tool), complete configurable device profile (DIS fields, firmware, battery level/drain/notify, advertising exactly like a real H10, HR curve/contact, PMD ECG/ACC), default + alternate profiles.
- 175 H10 fidelity ground truth (owner): h10-capture scenario (adv/GATT/values/timing distributions/behaviour probes) on real straps from phones + Tauri; sim timing model sampled from measured distributions (seeded); comparator equivalence test sim vs real; every modeled behaviour cites a source; UNCONFIRMED list. Queued after sim-full round (muse-h10-capture.md). Captures tonight.
- 176 FIXED (FX5): example-expo/scripts/build-tv.sh restage keeps a stale unified-ble-manager copy (pnpm reuses the cached file: package), so the TV app fails closed with protocol.incompatible native-identity; also staged AppDelegate defaulted to Metro 8081 (clashes with other project). Fix: stage must refresh the library copy (sync or --force reinstall) and honor TV_METRO_PORT in every step; test. FIXED (FX5: `cmd_verify_identity` in example-expo/scripts/build-tv.sh:151-170; TvBuildStage.test.js green; see the line-396 FIXED entry).
- 177 Linux host BLE advertising blocked by Ubuntu BlueZ 5.72 (ADD_EXT_ADV_DATA 8 extra bytes) vs kernel 6.8.0-139 strict length check (upstream fixes: kernel 149324fc762c, BlueZ 2a6968b40378). Options: boot 6.8.0-138, wait for SRU, or opt-in raw-MGMT advertising in h10-sim. Simulator should report this precisely and never retry-loop.
- 178 Secret-scanning alert #1: Google API key inside @react-native/debugger-frontend (Chrome DevTools public key) in the orphaned commit 4b0c1e52 (.pnpm-store accident, force-pushed away; .pnpm-store now gitignored). Owner decision pending: dismiss as wont_fix.
- 179 FIXED (FX5): Apple readiness says request-permission but permissions.request is unsupported (legacy had same) → implement Apple permission request via CoreBluetooth prompt, same result as Android; readiness actions must be actionable everywhere (Muse round muse-fix-apple-permission.md). FIXED (FX5: system prompt presented in ios/UnifiedBleExpoRuntime.mm:109-144; expo-permission-request tests green).
- 180 PHYSICAL Apple TV 4K (3rd gen) "Office", tvOS 26.5, strap E9B93D29 (2026-09-18 ~21:00 PDT), same shared scenarios: scan PASS (60 peers ~190 obs/s), h10-stream PASS, device-info PASS, mtu PASS (= iOS: write length measured; effective/request MTU, PHY, parameters unsupported), ECG 130 Hz PASS (0 gaps/drops/overflow), forced link-loss reconnect PASS (detect 0.14 s, reconnect 2.9 s, first value 0.9 s), background lease capability.unsupported (correct on tvOS). Before the owner accepted the H10 pairing prompt: ECG subscribe → platform.security CBATTErrorDomain#15 (correct vocabulary).
- 181 Live dashboard (owner): TV-first tiles per Polar strap (HR, RR, contact, live ECG trace, battery, firmware/model/serial), auto-reconnect states, controller in examples-shared + driver scenario live-dashboard; queued after Apple permission round.
- 182 FIXED (FX5): Tauri physical — database rejected protocol.violation public-gatt.duplicate-characteristic-path after connect (H10 E9B93D29); Node host OK with same core → Tauri/IPC database path defect; also the error lacks the offending path. Muse round muse-fix-tauri-gatt.md. Verified meanwhile: Tauri filtered scan now PASS (finding 170 fix confirmed physically). FIXED (FX5: duplicate-path defect fixed — `finding_182_*` tests at native/tauri/src/dispatcher_packet_b_tests.rs:2923 assert no duplicated characteristic path reaches validateTopology; the 'error lacks the offending path' sub-part is UNVERIFIED at HEAD).
- 183 FIXED (verified 2026-09-21: status 129 on a submitted CCCD write classifies as the stack's transient glitch — GATT_STATUS with the status kept and dispatched=true, never not-connected — android/.../rustcore/RustRadioHostAdapter.kt:942-946; the Rust core reports platform.failure caller-decides so the caller retries per vocabulary). Test: RustRadioHostAdapterTest 59/59 pass incl. cccdWriteInternalErrorKeepsStatusForCallerRetry + cccdWriteInternalErrorReachesCoreWithStatus (fresh gradle run). Original: OPEN: Android subscribe CCCD write status=129 (GATT_INTERNAL_ERROR) right after the previous scenario released the same strap; retry passed.
- 184 PHYSICAL final build (ef53981a + restoration) 2026-09-18 ~21:00: iPhone 16 Pro Max + Samsung on strap E9B93D29: stream, device-info, ECG 130 Hz (0 gaps), forced reconnect PASS on both (iOS detect 0.28 s/reconnect 3.0 s; Android 0.2 s/0.8 s after one 129 retry).
- 185 FIXED (verified 2026-09-21: a failed scan stop keeps its session membership for retry and the next start retries it instead of bricking the session — src/backends/reactnative/react-native-rust-core-provider.ts:1793, react-native-rust-core-manager.ts:557; capture stops its scan before find(), finding 209). Tests: **tests**/backends/reactnative/rust-core-scan-lease.test.js + rust-core-manager-f2b-fixes.test.js, 10/10 pass. Original: OPEN: Android scan.already-active on h10-capture after many prior scenarios; stopping every scenario did not clear it; fresh app process fixed it → leaked scan/ownership across scenario managers in one process.
- 186 Real-strap fingerprints captured (E9B93D29): iPhone + Samsung → tool/h10-sim/fixtures/h10-fingerprints/.
- 187 FIXED (verified 2026-09-21: chooser readiness is availability plus a supported chooser, never power — `adapterIsReady` returns true for `choose` once available/authorized; explicitly unsupported chooser fails closed at once — src/public/ble-manager.ts:2240-2267). Test: **tests**/public-adapter-readiness-cancellation.test.js 13/13 pass (finding-187 block: resolves without power, fails closed without chooser). The old harness workaround is already gone (no deferredToChooser anywhere; examples-shared/driver/host.ts cites finding 187). Original: OPEN (library): adapter.waitUntilReady({operation:"choose"}) on Web waits for authorization, which only the chooser can grant → times out.
- 188 FIXED (FX5) (Web physical, Chrome 152 macOS, H10 via chooser): connect OK then start fails gatt.not-found web-gatt.discover-descriptors (browser NotFoundError) — Web Bluetooth getDescriptors() throws NotFoundError when a characteristic has no descriptors; must be an empty list (check legacy). Peer name reported null after chooser. FIXED (FX5: NotFoundError → [] in src/web/web-bluetooth-gatt.ts:328-348; web-bluetooth-descriptor-absence green; see the line-396 FIXED entry).
- Web test page must be served from a static build during physical tests (vite dev HMR reloaded the page mid-chooser when Muse edited shared files).
- 177 update: <linux-sim-host> (Ubuntu 26.04, kernel 7.0.0-30, BlueZ 5.85) has the SAME failure (ADD_EXT_ADV_DATA plen 23 for 15 bytes of content) → BlueZ 5.85 still pads 8 bytes; kernel 7.0 still strict. Plan: h10-sim opt-in raw-MGMT legacy Add Advertising (0x003e) path (CAP_NET_ADMIN via setcap on the binary, documented per the elevated-privilege rule, rm-adv cleanup on exit, never silent) while GATT stays on bluetoothd; also report the BlueZ/kernel mismatch precisely.
- 189 FUTURE (owner): once UBM gains a peripheral role (#217), the H10 simulator should move onto UBM itself (Linux first), so UBM tests UBM; the mgmt-legacy advertising workaround and its lessons feed that design.
- 177 FIXED (tool): h10-sim --linux-advertising mgmt-legacy (raw MGMT Add Advertising 0x003e, CAP_NET_ADMIN via setcap, cleanup on every exit, stale-instance record); default bluez mode diagnoses the BlueZ/kernel bug and exits 78. PHYSICAL: Samsung found "Polar H10 SIM0001" on <linux-sim-host> (Ubuntu 26.04/kernel 7.0/BlueZ 5.85) and on <linux-build-host> (Ubuntu 24.04/kernel 6.8.0-139), connected, streamed HR (14 values @72 bpm, max gap ~1.08 s); no leftover instances. Kernel revert on <linux-build-host> no longer needed for the simulator.
- 190 FIXED (verified 2026-09-21: (a) a requested disconnect ends notifications owner-released — native/tauri/src/btleplug_dispatcher.rs:2111; IPC app-release delivers owner-released — src/ipc/manager.ts:1449-1507, so the supervisor reconnects like RN. (b) Tauri advertises gatt:maximum-write-length + connection:effective-mtu per-OS like the desktop core — native/tauri/src/capabilities.rs:91-131, owner decision J). Tests: cargo finding_190a + finding_190b 2/2 pass; jest **tests**/ipc/connection-release-lifecycle.test.js 2/2 pass. Physical re-proof on H10 still owed. Original: OPEN (Tauri physical, macOS, H10): (a) after an app-requested connection.disconnect the subscription terminal is stream.closed and supervisor STOPS. (b) gatt:maximum-write-length (and effective MTU) unsupported on Tauri while Node macOS reports it (decision J).
- 191 PHYSICAL Tauri macOS (after fixes 170/182), strap E9B93D29: filtered scan, stream, device-info (battery 90, fw 5.0.0), ECG 130 Hz 0 gaps, RSSI PASS.
- DECISION J (owner 2026-09-18): YES — modernize. 5.0 need not be 4.x-compatible; where the platform supports it, expose it (Tauri max-write/long-write/effective MTU like the desktop core). General rule: prefer the improved behaviour over 4.x parity whenever the platform supports it (still record the departure in CHANGELOG).
- 192 Raw capture (Tauri, E9B93D29): 120 HR packets + 45 ECG frames with timings → tool/h10-sim/fixtures/h10-raw/ (+ ecg-…-130hz.txt replay file, 3285 samples). h10-capture gained recordRaw. FIRST FIDELITY BUG: real H10 HR flags = 0x10 (RR present, contact NOT supported) vs sim 0x16 → fix in sim-vs-real round.
- 193 LATER (owner): H10 firmware update / Polar file-transfer (PSFTP) simulation — post-5.0. Accelerometer (PMD ACC) capture + sim: when needed (one-minute capture).
- 194 FIXED (FX5) (Node macOS host vs SIM0001): first h10-stream start after host launch failed connection.already-owned (direct-gatt.connect, desktop-rust-core) — no prior connection by this host process; investigate (stale arbitration from a failed scan? OS-level connection held by another local process such as Tauri/Electron on the same Mac?) and whether the name is right per vocabulary. FIXED (FX5: deadline/abort cancels the backend via backendAbort at src/core/unified-ble-core.ts:579-608, deadline → `connection.failed`; connect-deadline-cancels-backend green; see the lines-408-409 FIXED entries).
- 195 Simulator OTA defects vs real stack (ambiguous 180A, PMD indication timeout, service-changed on drop-link, scan gap) → Muse slice SIM1 (tool/h10-sim only).
- 196 FIXED (FX5) (flake): **tests**/electron/electron-adapter-recovery.test.js "main rebinds the renderer and the supervisor reconnects after a >10 s outage" failed once under full test:package load (announced rebind not observed), passes 5/5 alone → make deterministic. FIXED (FX5: the test now pins the announced-rebind signal explicitly — toHaveLength(1) + matchObject backend-restarted — and is green in isolation 2/2; see the 197-204 FIXED entry).
- 176, 188: FIXED (verified by review R2B/R2C at 36187bf1: build-tv.sh refreshes the staged copy + identity check + TV_METRO_PORT; web getDescriptors NotFoundError → []).
- 197-204 REVIEW WAVE R2 (Muse xhigh, 36187bf1): HIGH IPC renderer without-response write rejected as malformed (src/ipc/manager.ts); HIGH supervisor stops when IPC/Tauri lifecycle iterator ends bare; MED Tauri commitState accepted vs contract unknown; MED Electron main-binding unbounded release retry timer not cleared on destroy; MED RN adapter-watch close failure swallowed on destroy; MED RN connect host errors relabelled connection.failed; MED 185 scan survives all manager destroys; LOW RN `as unknown` casts, pump-failure cleanup record dropped, stale CHANGELOG prebuilt line. Fix slices F2A (desktop/IPC) + F2B (RN) running.
- 195 FIXED (SIM1): single 180A (<linux-sim-host> /etc/bluetooth/main.conf [General] DeviceID=false — reversible system change), PMD indications confirmed per command (ECG/MTU OTA OK), drop-link keeps GATT app (terminal connection-lost), advertising persists across drop-link/stop-start. Known limit: ATT handle order follows dbus-crossroads HashMap (compare by service set).
- 205 FIXED (FX5) (library, desktop macOS Node/Electron): duplicates:all filtered scans stop re-reporting a peer (SIM0001) after it has been connected a few times, while a fresh name is found instantly → known-peer re-observation staleness (findings 120-122 area). Needs a real-layer test + fix. FIXED (FX5: `reobserve_known_peers` in crates/ubm-desktop/src/central.rs:5479 + btleplug_backend.rs:2226; see the line-401 FIXED entry).
- 197-204 FIXED (F2A/F2B, 2026-09-19): IPC without-response receipts unknown everywhere; IPC app-release delivers owner-released → supervisor reconnects like RN; Tauri commitState contract word; Electron release retry bounded + disarmed; RN watch-close failure reported; RN host errors not relabelled; RN casts replaced; pump cleanup traced; 185 pinned by N-manager test; 196 flake made deterministic. Test seam moved to /testing (decision L).
- 205 FIXED (F3): root cause = CoreBluetooth GAP-name poisoning after connect (scan name became the peripheral GAP name, e.g. host "<linux-sim-host>") + no known-peer re-observation outside BlueZ; merged/nameless sightings track the advertised name; every desktop scan re-observes known peers once. OTA: 9/9 connects, name correct.
- 206 FIXED (verified 2026-09-21: interval is set by the sim itself via MGMT ADD_EXT_ADV_PARAMS — tool/h10-sim/src/mgmt.rs:95-99; adapter GAP alias is claimed while running and restored on exit — tool/h10-sim/src/bluer_radio.rs:700-815. Tests: cargo mgmt 22/22 pass incl. the (1600,1600) interval assertion. Physical closure is finding 219: an independent Linux controller captured the nominal 1000 ms cadence over the air). Original: OPEN (tool): h10-sim mgmt-legacy advertising is kernel-paced at ~0.1 Hz observable (neighbors ~5 Hz) → set the interval via MGMT ADD_EXT_ADV_PARAMS + correctly sized ADD_EXT_ADV_DATA from the sim itself; set the adapter GAP alias to the profile name while running (restore on exit).
- 207 SIMR: sim matches iPhone/Tauri captures exactly (Android: platform 1800/1801 only); HR flags 0x10, NUL DIS, strap identity, 17-byte PMD features, 73-sample ECG from the real trace, HR replay, measured timing. OTA sim fingerprint still pending.
- 208 FIXED: iOS app build broke after 179 (ObjC called Swift requestPermission with the Swift spelling; generated header needed CoreBluetooth import); the Apple native-protocol gate never compiles UnifiedBleExpoRuntime.mm against BlePlx-Swift.h → new guard **tests**/AppleExpoRuntimeSwiftSelectors.test.js (RED on the old call). CI apple job builds the example and would catch it too.
- 210 PHYSICAL vs SIM0001 (<linux-sim-host>, 1 s advertising, 2026-09-19 ~06:10): Samsung scan/stream/ECG 130 Hz/link-drop PASS (peer-link-loss → connection-lost → reconnect 1.7 s), device-info connection.failed once (retryable); Electron scan/stream/device-info (fw 5.0.0, batt 90)/ECG PASS, link-drop lost+connection-lost PASS but no reconnect within 15 s; Node scan/stream/device-info PASS, link-drop PASS (reconnect 5.1 s), ECG connection.already-owned right after device-info released the peer (194 reproduced); Tauri stale binary → rebuilt.
- 209 FIXED (F4): capture stops its advertisement scan before find(); scan.already-active names the occupying scan (desktop, RN, central). OTA: node capture vs SIM0001 exit 0, no already-active.
- 211 FIXED (F5): released-handle tombstones in the Electron main router; OTA electron link-loss vs SIM0001 reconnected gen-2 in 3.1 s, 62 values after.
- 194 FIXED at the desktop backend (F5): a newer connect supersedes the in-flight acquisition; OTA node device-info → ecg connects in 1.4 s. Root remains: UnifiedBleCore.connect settles its deadline without cancelling the backend call; RN and Tauri arbitration must be checked for the same race (F7).
- 194 ROOT FIXED (F7): core connect deadline/abort cancels the backend via the ConnectionOptions signal; deadline → connection.failed; RN provider supersedes like desktop; ubm-mobile kernel proven to free claims (cargo test); Tauri already routes operation.cancel.
- 212 FIXED (example config): root cause is WKWebView suspending the occluded window's page (socket still pongs, JS paused; fronting the window answered in 2 ms). example-tauri main window backgroundThrottling "disabled", guard test tauri-host-window.test.mjs. Original report: Tauri host (rebuilt, restarted via hosts.sh) answers its first scan, then every later command times out at the driver (120 s) with no host log output; not window occlusion (brought to front, still hung). Needs its own investigation (webview console, plugin dispatcher, scenario busy state).
- 213 FIXED (F8): scan-start re-observation burst (baa065fa) overflowed find's capacity-1 budget because Tauri IPC filters after bounding; drop-policy overflow is loss accounting in find/scan.events; stream.overflow caller-decides with platform detail. Live Tauri host finds SIM0001. Original: Tauri find() fails stream.overflow ~100 ms after start (visible or hidden window), scan-details sees nothing; the error says retryability never but recovery retry-with-backoff, platform null.
- 214 FIXED: build-tv.sh install without a TTY — pnpm modules-purge prompt emptied node_modules and exited 0 → TV "Unable to resolve unified-ble-manager/expo". Purge confirmed up front plus presence check; TvBuildStage test.
- 215 NOTE: iPhone app after the F4/F7 Rust changes failed closed protocol.incompatible native-identity (sourceDigest) — correct fail-closed behaviour; rebuilt the Apple Rust core.
- 216 ROOT-CAUSED (W3: sim sent ECG frames before the deferred PMD START indication; two-phase commit fix); iPhone end-to-end proof pending (W3B) (tool): h10-sim ECG vs iPhone fails (connection.lost CBError 7 after the first 229-byte frame; later stream.closed) while Android/TV/Electron stream 130 Hz from the same sim, and the iPhone streams 37 frames/20 s at 130 Hz from the real H10 E9B93D29 → simulator fidelity with iOS; capture btmon on <linux-sim-host>.
- 217 FIXED for iOS/tvOS (W5, limited, derived maxWriteValueLength+3); desktop macOS/WinRT/BlueZ + Tauri in W5B (library, Apple): effective MTU reported capability.unsupported on iOS, but CBPeripheral.maximumWriteValueLength(for:) exposes ATT_MTU-3 → modernize (same answer as Tauri decision J).
- 218 PHYSICAL real H10 E9B93D29 ECG 20 s (2026-09-19 ~09:25): Android 36 frames 130.0 Hz, Apple TV 37, Electron 33, iPhone (unlocked) 37, all 0 gaps; iPhone locked earlier gave 3 frames (iOS background throttling, expected); Node refused protocol.incompatible (stale N-API after agent edits → F9 lifecycle).

## External review (owner-supplied, of 36187bf1, received 2026-09-19) — verify each against HEAD before fixing

- X-R1 FIXED (W1, confirmed at HEAD) P1 mobile join_scan ignores OpControl cancellation/deadline while queued on the scan mutex (host.rs join_scan, session.rs ScanStart). → W1
- X-R2 FIXED (W1, confirmed) P2 two concurrent ScanStart in one mobile session can both succeed (no reservation before await). → W1
- X-R3 FIXED (W1, confirmed; per-peer connect section across stage→dispatch→cleanup) P1 connect options staged by peer id, not operation (foreign.rs stage_connect/clear_staging). → W1
- X-R4 FIXED (W2, confirmed; cold start installs the owner and delivers restored records; no native auto-connect by contract; physical runbook pending owner) P1 Android presence restoration stops at persistence; no cold-start owner bootstrap after process death (UbmCompanionPresenceService, PresenceWakeCoordinator, RustCoreProcessHost). → W2 (#212 scope)
- X-R5 FIXED (W1; cumulative controlLost in every drain) P2 control-loss ingress-drop record postponed until both queues are empty (drain.rs Outbox::drain). → W1
- X-R6 FIXED (W1; Signals cap 1024, per-scope coalescing, overflow reconciles) P2 host Signals VecDeque unbounded for non-coalesced classes (host.rs Signals::push). → W1
- X-S1 FIXED (verified 2026-09-21: drop-link touches only tracked GATT-observed simulator clients plus the explicit --drop-link-allow list — note_client/prune_client wired on connect/read/write paths, tool/h10-sim/src/bluer_radio.rs:121-135,391,437-504,862,1207; drop_targets union in tool/h10-sim/src/radio.rs:342; faithful/adversarial modes + run-record exist in tool/h10-sim/src/control.rs:99-241). Tests: cargo sim radio 10/10 pass incl. drop_targets_union_clients_and_allowlist_sorted. Original (W3 made it allowlist-only; W3B scopes to GATT-observed clients): P1 sim drop-link disconnects every device connected to the adapter.
- X-S2 FIXED (W3: bounded ordered pump, install-before-announce, generation ledger; spawn already fixed) P1 sim notify returns Ok before delivery (spawned send, unbounded, unordered); writer lifecycle races (readiness before writer install, generation-blind cleanup). Possibly explains 216. → W3
- X-S3 FIXED (W3: Polar epoch default, --clock unsynchronized) P2 sim ECG timestamps use Unix epoch; Polar epoch is 2000-01-01 (offset 946684800000000000 ns). → W3
- X-S4 FIXED (W3: passed vs complete, parents, mfr payload, tails, schema allowlist) P1 sim compare "passed" ≠ complete: skipped timings pass, p50 only, manufacturer payload unchecked, GATT parents dropped, missing==missing, version not schema-checked. → W3
- X-S5 FIXED 5/6 (W3); stop-advertising already fixed. sim small: apply_rates partial mutation; PMD delay blocks the event loop; control server readiness before bind; unbounded TCP line/no auth deadline; live profile change commits before radio success; stop-advertising releases the GATT app. → W3
- X-CI1 FIXED (W3) Linux sim fixture freshness: hello osVersion not normalized (platform is). → W3
- X-CI2 FIXED (orchestrator; verified binding emits connection.failed core/deadline-expired) bindings/napi/js/dispatch_roundtrip.cjs pins operation.timed-out for a dispatched connect deadline; contract is connection.failed (finding 161, F7). Do not accept both. → W4

- W6 (review section 5): deterministic acceptance scenarios 1–6 across backends, driver scenarios for 1/4/5, and an RN throughput benchmark — in progress.
- W3B: faithful/adversarial simulator modes with run records — in progress.
- 219 PHYSICAL / FIXED (2026-09-21): an independent Linux BLE controller captured 20 raw LE Extended Advertising Reports from SIM0001 during a 58 s active scan. Deduplicating advertisement/scan-response pairs yielded 10 observed events; the consecutive gaps included 992 ms, 1003 ms and 1007 ms, and every longer gap rounded to an integer multiple of one second. This is external HCI evidence of the configured nominal 1000 ms cadence; RF/scan loss explains the missed multiples and means the capture does not claim every advertisement was received. Original: OPEN (tool/environment, low): SIM0001 advertising configured at 1000 ms observed at 0.25–0.66 obs/s across hosts; verify the real over-the-air interval with btmon.
- 220 PHYSICAL vs SIM0001 after the review wave (2026-09-19 ~17:00, commit be056fdc): Electron/Node/Tauri/AppleTV all pass stream, ECG (25 frames, 130.0 Hz, 0 gaps) and link-drop with IDENTICAL vocabulary (peer-link-loss → connection-lost → reconnect: 1117/1112/1143 ms desktop, 3172 ms TV). device-info transient failures on Electron (connection.failed) and TV (driver.command-timeout) both passed on immediate retry. iPhone pending (owner brings the phone ~19:00).

## External expert review #2 (owner-supplied, of be056fdc + CI run 35474629985, received 2026-09-19 ~18:00)

Confirms R1, R2, R3, R5, S1, S2, S3, S4-comparator, the Linux CI fixture and the NAPI vocabulary as genuinely fixed (re-read, not taken on trust). Remaining, each assigned:

- Y1 FIXED (FX6): test-client identity is now a process-monotonic id (RED test reproduced address reuse); production admission untouched; no other pointer-derived persistent key exists. Original: Y1 P1 CI RED, Rust 5.0 workspace (windows-latest): a_lost_adapter_ends_live_work_and_advances_the_generations_once_per_episode fails argument.invalid/ubm-mobile.wire.args.admission. Test-harness defect: the helper keys a process-global Clients map by session.instance_key() = Arc::as_ptr(state), unique only while that session lives; the [Android, Apple] loop drops one session and the Windows allocator reuses the address, so the new session inherits stale admission numbers and production correctly rejects them. Fix the TEST identity (monotonic id), never the admission check; sweep for other pointer-derived persistent keys. → FX6.
- Y2 FIXED (FX6): one queued marker plus a dirty-scope set with a bounded batch drain (RED: 6024 scopes queued 6024 entries; now queue ≤ cap+2, nothing stranded). Original: Y2 P2 R6 REOPENED: SIGNALS_CAP 1024 is bypassed by HostSignal::Value(scope) ("always queues, past the cap if it must"), so the queue is not hard-bounded when many distinct value scopes go dirty under a stalled pump. Fix per the reviewer: one queued marker plus a dirty-scope set, pump drains a bounded batch and requeues while more remain. → FX6.
- Y3 FIXED (FX7): structural tests renamed and marked partial; new --qualify-ota requires passed AND complete (RED: in-process fingerprint names 24 unverified fields). Original: Y3 P2 fidelity evidence: sim*fingerprint_matches_the*{tauri,ios,android}\_capture assert report.passed but never report.complete, while the in-process fingerprint omits central-side timings and yields n:1 distributions. Split structural (passed, named as partial) from OTA qualification (passed && complete). → FX7.
- Y4 FIXED (FX6): connect_sections count exposed as counters.describe process.native.connectSections. Original: Y4 P3 R3 caveat: connect_sections retains one mutex per peer ever seen (monotonic); expose its count in diagnostics so a soak can prove it is not growing from garbage peer ids. → FX6.
- Y5 QUALIFIED 2026-09-20 (clean cycle with the owner): app crashed 13:06:34 (process gone), strap returned 13:11:58 -> CDM bound UbmCompanionPresenceService into the DEAD app (new pid 20372), app opened -> `restoration restored` count 1, sources ['restored'], opaqueId <strap-address>. 5m24s with no app process; no scan, no user action. Original: R4 status is "code fixed, hardware qualification outstanding": Android cold-process restoration needs the physical runbook run (owner; W2 receipt has the procedure). → owner.
- Y6 CI Expo CNG Android build failed on Maven Central HTTP 403 (environmental; Expo typecheck, doctor, prebuilt verification and prebuild all passed before it). Rerun rather than change product code. → orchestrator.
- Y7 FIXED (verified 2026-09-21: s5 now drains to quiescence with bounded pulls and asserts conservation against the final cumulative loss notice, never one notice — **tests**/acceptance/w6-reliability-acceptance.test.js:709-749). Test: s5 holds on 6/6 runnable legs here (38 skipped: platform legs unavailable on macOS); the winrt leg that flaked on ubuntu/windows CI needs a green CI run as final proof. Original (orchestrator): leg desktop-winrt "s5-slow-drain-bounded" RED on ubuntu/windows, passing on macOS.
- Y8 Reviewer's release gate: the whole deterministic CI matrix must be green before CI is used as evidence for 5.0 readiness. → orchestrator, after FX1B/FX6/FX7 land.
- Y9 DONE (GTV1) — Google TV emulator API 36 is a repeatable host (example-expo/scripts/android-tv-emu.sh, 7 contract tests). Phone APK runs unmodified; evidence ceiling is adapter/scan truthfulness (0-observation scan, no RF), never physical-radio proof. Every unrunnable op failed closed truthfully (permissions → scenario.bluetooth-not-ready; find → operation.timed-out caller-decides). Physical Google TV runbook in the receipt. Original ask: add Android TV / Google TV to the test matrix — it supports Bluetooth natively. Emulator images present on the Mac (TV_IMAGIBOOKS_GOOGLE_TV_API_36_arm64_v8a, TV_IMAGIBOOKS_ANDROID_TV_API_36_arm64_v8a); no physical Google TV attached yet. → GTV1 slice: make it a first-class driver host, establish by experiment what emulator Bluetooth proves (adapter/capability truth, never physical-radio evidence), one-command repeatable script plus a contract test, and the runbook for physical hardware when available.
- Y10 (owner): review cycles continue until a review finds nothing. Each wave: verify-then-fix slices, full gates, green CI matrix, then a fresh independent review of the fixed tree.
- 221 PHYSICAL iPhone 16 Pro Max vs REAL Polar H10 E9B93D29 (2026-09-20 ~10:05, app rebuilt on 73509252): scan sees the strap; HR stream 88 bpm; device-info fw 5.0.0 / battery 100%; ECG 37 frames, 2701 samples, 129.97 Hz, 0 timestamp gaps; link-loss (walk-away) TWO outages, both recovered — detect 11957/1566 ms, reconnect 22776/14094 ms, first value after reconnect 1365/1342 ms, generations 1→3→4, terminals connection-lost, 51 values. Vocabulary identical to Android/Electron/Node/Tauri/AppleTV. iOS reconnect is slower than desktop (~1.1 s) — iOS scan throttling plus supervisor backoff, a measured platform difference, not a defect.
- 216 PROVEN 2026-09-21 (physical iPhone 16 Pro Max vs h10-sim SIM0001, faithful mode, simulator relocated to <sim-host-2> because the first host's Bluetooth adapter is absent): 74 frames, 5402 samples, sensorSampleRateHz 130.0000000, timestampGaps 0, estimatedMissingSamples 0, missingNotifications 0, overflowNotices 0, droppedItems 0, parseFailures 0, maxFrameBytes 229; clean PMD stop (status SUCCESS) and every cleanup step released. The W3 root cause (frames before the deferred PMD START indication) is closed: the earlier `connection.lost` CBError 7 after the first frame does not recur, and iOS now matches the Android/TV/Electron behaviour from the same simulator. NOTE for anyone re-running: the second host carried a simulator binary predating the W3 fix — running it would reproduce the old failure and read as a regression. Original: the iPhone-vs-simulator ECG proof could not run — the Linux simulator host <linux-sim-host> (<linux-sim-host>) is DOWN (ssh "Host is down", sim absent from the driver). The iPhone scan saw a stale cached advertisement and every connect timed out, which is the host being down, not a library defect. Retry when the machine is back: ecg start vs SIM0001, 20 s, expect ~36 frames at 130 Hz.
- 222 FIXED (FXF; physically proven 2026-09-20 with the owner: after the BluetoothLeDeviceFilter fix the chooser listed the strap and associated it — dumpsys shows Association mId=4 'Polar H10 E9B93D29' <strap-address> for the example package). Original: P1 (library, Android, found with the owner 2026-09-20): companion association cannot select a BLE peripheral. `ReactCompanionChooser.kt:74-77` builds `android.companion.BluetoothDeviceFilter` (CLASSIC Bluetooth) instead of `BluetoothLeDeviceFilter`, so a BLE-only peer (Polar H10) never appears in the chooser; with `setSingleDevice(true)` and no name pattern the dialog offers the first classic device it finds and the user associates the wrong device (observed: "S39 4BEA LE" and a bare MAC associated to the example package). Consequence: Android presence observation and cold-start restoration (#212, X-R4) cannot be armed by a real user at all. Fix the filter class, keep the name/serviceUuid pattern path, and cover it with a test; also give the example's Diagnostics action the H10 name so the chooser is scoped.
- 223 FIXED (FXF peer ids, FXH scan/connection/database/subscription ids minted from one place; android-_ on Android, corebluetooth-_ only on Apple; verified on the phone: android-peer-1-29). Original: P2 (library, Android): the Android host reports peer ids prefixed `corebluetooth-peer-N` (observed on expo-android-samsung-sm-a376u1-2 via scan-details and restoration start). The shared kernel's radio naming leaks a CoreBluetooth identity onto Android; a consumer sees identifiers claiming a framework that is not on the device. One vocabulary means the id names the platform that produced it.
- 224 FIXED (FXG; physically proven 2026-09-20 on <linux-sim-host>: iptables-dropped the driver port, sim left the host list, logged "rejoining in 1290ms (next attempt 2)", and re-registered by itself when the rule was removed). Original: P3 (tool): after the Linux host rebooted, h10-sim kept advertising but never rejoined the driver (`--driver ws://...`); it needs a bounded reconnect with backoff, and its log should say it is retrying.
- 225 FIXED (FXI: observe-presence/unobserve-presence driver commands + Expo Diagnostics actions; hardware answer {"state":"observing"}). Original: P2 (harness + example, found with the owner 2026-09-20): presence observation can only be armed through the API (`observePresence({peerId})`, src/expo.ts:726), but NOTHING exercises it: the driver's restoration scenario only REPORTS `state:presence-observation` capability (examples-shared/driver/scenarios/restoration.ts:52,109) and the example's Diagnostics screen offers association only. So the Android cold-start restoration feature (#212, X-R4, W2) cannot be armed by us or by a user, and its physical acceptance test cannot run. Add a driver command (restoration observe/unobserve presence) and an example action, then run the physical runbook.
- 226 FIXED P1 (library packaging, found 2026-09-20 on the physical Samsung): android/src/main/AndroidManifest.xml never declared REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE, so presence observation answered permission.denied in EVERY consuming app and Android cold-start restoration (#212, X-R4) could not be armed. Declared in the library manifest (merges into the consumer app), pinned by **tests**/AndroidPresenceManifest.test.js.
- 227 FIXED (FXJ; plugin declares the permission with the service and removes it when mode is none, 4 RED-first plugin tests, example native project regenerated; hardware: observe-presence -> {"state":"observing"} ok, unobserve -> idle). Original: P1 (expo plugin + example, found 2026-09-20 on hardware): the presence feature is not wired end to end for an Expo app. (a) plugin/src/withBLEAndroidCompanionPresence.ts adds the CompanionDeviceService when background.android.mode != "none" but NEVER adds `REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE`, so every Expo consumer gets permission.denied from observePresence (226 fixed only the bare-RN library manifest). (b) example-expo/android/app/src/main/AndroidManifest.xml contains neither the service nor the permission — the app has not been prebuilt since the plugin gained presence, so the OS has nothing to bind and cold-start restoration cannot work there. Fix the plugin (with plugin tests), re-run prebuild for the example, rebuild/install, and prove `restoration observe-presence` answers observing on hardware.
- 228 FIXED (orchestrator): the RN provider now resolves a known opaque peer id to its native address for presence.observe/unobserve, and passes an unrecognised id through unchanged so the native seam and the TCK (which hold addresses) keep working. RED proof: reverting the resolver fails the new test. Original: P2 (library, Android, found 2026-09-20 on hardware): `presence.observe` takes the public opaque peer id but the native path passes it straight to `startObservingDevicePresence`, which requires a MAC address: arming with the id the library itself hands out (`android-peer-1-11`) fails `platform.failure` "android-peer-1-11 was not a valid MAC address", while the raw address works. The public id must be resolved to the platform address inside the owner (or the contract must say an address is required, which would break the opaque-id rule). Same question for unobserve and for any other API taking a peer id on Android.
- 229 FIXED (FXM: the wake path now logs each delivered appearance; 40 live appearances showed one 'presence wake delivered' line each). Original: P3 (observability, Android): the presence wake path logs ONLY on failure (UbmCompanionPresenceService TAG "UbmPresenceService", Log.w on errors; RustCoreProcessHost "UnifiedBleRustCore"). A successful OS wake therefore leaves no trace at all: the 2026-09-20 physical run showed CDM binding the service in a dead app with zero lines from our code, so success and "the handler never ran" look identical in the field. Add one info line per wake (appearance received, owner installed or already installed, ingested vs persisted, count), and cover it in the Robolectric test.
- 230 PHYSICAL Android cold-start wake (2026-09-20 12:41-12:42, app killed via `am crash`, pid 14586 -> gone): with presence armed (observe-presence <strap-address> -> observing), the OS reported the strap lost, then found, and BOUND UbmCompanionPresenceService into a dead app (CDM_CompanionAppBinder "Binding package=com.sfourdrinier.bleplxexample", new pid 15135, service-only, no JS/UI). `am kill` alone does NOT kill the app while CDM holds the service binding — use `am crash` (never force-stop, which disables the wake).
- 231 FIXED (orchestrator: ScreenDefaultContainer gained an opt-in scrollable prop; the Dashboard's controls moved into its peer list's header, so one scroller and no nested virtualized list). Original: P3 (example app, reported by the owner 2026-09-20): example-expo screens do not scroll (no ScrollView), so actions below the fold are unreachable on a phone — the Dashboard's "Claim native restoration" button could not be tapped on a Samsung SM-A376U1. The example is the reference a consumer copies and the only way to exercise several features by hand; make the screens scrollable (ScreenDefaultContainer or per-screen ScrollView) and check every screen at phone height.
- 232 FIXED (FXK: driver `restoration restored` queries the public peers.restored directory, example gains a 'Show restored peers (Android)' action, iOS claim labelled '(iOS)'; docs/BACKGROUND.md states the platform difference. Physically observed on the Samsung: count 1, sources ['restored'], opaqueId <strap-address>). Original: P2 (harness + example, found on hardware 2026-09-20): the Android restored peer is unobservable. Android has no restoration journal: `restoration.claim()` is correctly `capability.unavailable` there (observed on the phone), and the OS-wake appearance surfaces through `peers.restored` (docs/BACKGROUND.md:28-35). But the driver's restoration scenario never queries `peers.restored`, and the example app offers only the iOS claim button — so a cold-start wake cannot be verified by us or demonstrated to a consumer. Add a `restored` command to the scenario and an example action, then re-run the cold-start acceptance (#212, X-R4).
- 233 RESOLVED (FXL: `name: null` is CORRECT — a restored peer has no advertisement observation yet, and the CDM association display name is an association fact, not a radio observation, so it is not merged silently; Apple passes through what it observed. Type docs state it). Original: P3 (observation, Android): a restored peer carries its address but `name: null` (physically observed: `{"id":"android-peer-1-1","name":null,"sources":["restored"],"reference":{"opaqueId":"<strap-address>"}}`). Decide whether the name is genuinely unknown until the first advertisement (then say so in the type/docs) or whether the associated CDM display name ("Polar H10 E9B93D29") should ride along; a consumer showing restored peers has only a MAC to display today.
- 234 IN SCOPE FOR 5.0 (owner 2026-09-20: "UBM 5.0 is a new major upgrade to enable all of those things"; background resume IS required, and the demo must show it working wherever the platform supports it; design slice BG1 running): after an OS presence wake the app process returns SERVICE-ONLY — the presence service installs the radio owner and records the restored peer, but starts no JavaScript, does not connect and does not resubscribe (deliberate: reconnect policy is the application's). So streaming does NOT resume in the background; it resumes when the app next opens a session. To make data resume without the user opening the app we would need either (a) a HeadlessJsTaskService started from the presence callback (Android supports it; the package registers none), or (b) a native auto-reconnect in the wake path, which contradicts the current contract. Note also that BLE gives no "new data" wake at all: the OS wakes on ADVERTISEMENT APPEARANCE, and data only flows over a live connection. Whatever is chosen, docs must state it plainly; today no document answers "does my app resume streaming by itself?".
- 235 ANSWERED for (a) 2026-09-20 on hardware, Samsung SM-A376U1: CDM presence observation SURVIVES both a reboot AND an OS update (phone updated to Android 16, build A376U1UES5AZH1, rebooted; nobody opened the app). Logcat at 13:27:25-26: `onBleCompanionDeviceFound associationId(4)` -> `CDM_CompanionAppBinder Binding package=com.sfourdrinier.bleplxexample` -> `ActivityManager Start proc 8674 ... for service .../presence/UbmCompanionPresenceService`. So an app arms observation ONCE; it does not need to re-arm per boot. The CDM association also survived. (b) the foreground-service restart policy is still unexercised. Original: (verification): reboot behaviour is untested. (a) Does the CDM `startObservingDevicePresence` registration survive a phone reboot, or must an app re-arm on every boot? (b) `background.android.restart: "while-session-intent-exists"` adds RECEIVE_BOOT_COMPLETED and a recovery receiver that restarts the configured foreground service after boot/package replacement (never scans or reconnects) — never exercised; the example leaves restart at the default "never". Both answers belong in docs/BACKGROUND.md as a "what survives what" table (process death vs app update vs reboot vs force-stop).
- 236 FIXED (FXM: associate pre-checks getMyAssociations and reports already-associated instead of creating a second; the wake is idempotent per MAC; companion list/disassociate added to the public surface. Physical: 40 appearances -> exactly one wake each, 0 double-wakes; leftover duplicate removed with the owner). Original: P2 (library, Android): duplicate companion associations accumulate. After this session the example package holds TWO associations for the same strap (mId=4 and mId=5, both <strap-address>, both display name "Polar H10 E9B93D29"), and the OS fires an appearance for each (logcat shows onBleCompanionDeviceFound for 4 and 5 back to back), so the wake path runs twice per appearance. `association.associate()` should notice an existing association for the same device and reuse it (or report it), and `presence.observe` should be idempotent per peer. Check what disassociate offers, and whether a consumer can enumerate associations through the public API at all.

## CI triage of the 5.0.0 branch (2026-09-21)

Seven jobs were red on `15175c09`. Three independent causes, not one.

- 237 FIXED (`49073358`): the canonical packed-tarball verifier had never run to
  completion — earlier failures always aborted the job before it — so three gaps
  in `scripts/ci/verify-package-tarballs.js` surfaced only now: the
  `backends/desktop` sources were missing from the shippable-source regex; five
  private runtime modules the Node/desktop entrypoints require
  (`native-build-identity-check`, `backends/legacy-native-require`,
  `desktop-core-addon`, `desktop-rust-core-exports`, `node-desktop-manager`)
  were in no list; and `scripts/release/native-build-identity.js` plus
  `vendor/**`, both declared in package.json `files`, were outside the archive
  allowlist. Verified against the packed rc.0 tarball with the prebuilds
  stripped (the complete matrix exists only in CI).
- 238 FIXED (`20242083`, `bc254c8a`): `src/generated/native-build-identity.ts`
  was stale, failing `pnpm prepack` in both Android jobs and both iOS compile
  checks. Cause: `15175c09` applied `cargo fmt` to `bindings/uniffi/src/lib.rs`,
  and the identity seals a digest over the Rust sources, so a formatting-only
  edit stales it. `native:refresh` rebuilds artifacts but does not write this
  file; that asymmetry is now documented in `docs/NATIVE_ARTIFACTS.md`. The
  "no Xcode 26.4+ app found" line in the Apple job log is the runner echoing the
  step script, not a failure.
- 239 FIXED (`503595da`): nine tests in three shell-script suites
  (`AndroidTvEmu`, `TvBuildStage`, `NativeConsumerGuards`) had never passed on
  windows-latest. All three causes were real bugs and none a platform limit, so
  nothing is skipped: the `TV_STAGE_DIR` guard knew only `${TMPDIR:-/tmp}`,
  which Git Bash leaves unset, and did not normalise a `C:\…` stage path for
  rsync; one test narrowed PATH so far that Node could not find `bash` itself,
  producing ENOENT with empty stderr which the test read as "the script said
  nothing"; and the harness's `pnpm` stub was extensionless, which Windows
  cannot execute. 26/26 green locally; windows-latest is the true gate.
- 240 FIXED (`5788092c`): `background.continuation` shipped with no
  documentation outside `docs/UNIFIED_SEMANTICS.md`. `docs/BACKGROUND.md` now
  documents the declared standing order, the four strategies, declaration from
  Expo `app.json` or the bare React Native host factory, `continuation.status()`
  and `claim()` with its loss accounting, the four `background:*` capabilities,
  the Android association/presence prerequisites and who owns the entitlements;
  `docs/EXPO_PLUGIN.md` documents the plugin option and the manifest meta-data
  it writes.
- 241 NOT A UBM DEFECT (diagnosed 2026-09-21): the bare RN example grew to 75 GB
  overnight in the iOS 26.5 simulator. Reproduced: 16 MB to 2.9 GB in under four
  minutes. 226,740 copies of `Failed to call into JavaScript module method
RCTDeviceEventEmitter.emit(). Module has not been registered as callable.
Registered callable JavaScript modules (n = 0)` in 75 s. The app talks to Metro
  on port 8081, which another project held, so it loaded a foreign bundle,
  registered no callable modules, and React Native then threw on every native to
  JavaScript call without bound. Open (example tooling): the Expo example uses
  8082 and the TV one 8083 to avoid exactly this; the bare example still defaults
  to 8081 and says nothing when someone else owns it.

## Finding 234 — first hardware exercise of the background wake (2026-09-21, Samsung SM-A376U1 + real Polar H10 E9B93D29)

The declared `native` standing order was exercised against a radio for the
first time. The result is a partial proof with a specific new defect, not a
pass.

**242 PHYSICAL / FIXED (library, Android, closed 2026-09-22 on hardware): the
native continuation wake completes and its queued values drain after the app
opens.** The earlier direct-connect/discovery-budget fix reached the radio, but
physical reruns exposed two more exact boundary defects:

- continuation declarations use public 1-based GATT occurrences while the Rust
  mobile wire and discovered database use 0-based occurrences. The wake asked
  for `180D#1/2A37#1`, so a successful discovery still ended as
  `gatt.not-found: path.resolve`. The executor now converts at the wire boundary;
  a RED/GREEN test pins public `#1/#1` to wire `#0/#0`;
- a manifest-declared order intentionally leaves the JavaScript manager option
  absent. Claim validation used that empty local option after native had already
  drained the session, rejecting the valid `ubm-continuation-0`. The provider
  now validates consumer indices against `consumerCount`, captured from the
  exact continuation session and returned atomically with its claim. This
  remains correct if the standing declaration changes before an older wake is
  claimed. Manifest-only and partial-subscription RED/GREEN tests pin the path.

The continuation executor now validates a held wake against the Rust owner's
`session.reconcile` snapshot before reporting it as already continuing. The
snapshot must show the peer connected with a current database and every active
consumer route live; otherwise the wake reconnects with fresh numeric consumer
ordinals. The session retains an append-only selector map for queued records,
and its claim returns that map with the batches. A changed selector, order, or
occurrence is refused while the previous session remains claimable, so mutable
declaration state cannot relabel an older backlog.

Physical closure used the Samsung SM-A376U1 and real Polar H10 E9B93D29. A
controlled absent-to-present transition started a service-only process with no
JavaScript; `UnifiedBleRustCore` logged `continuation completed ... connected
direct, resubscribed 1` about 1.6 seconds after the service connection. Before
the UI opened, the service logged `queuedBytes=15` with zero native ingress
drops. `continuation.status()` then reported `continuation.completed`, and the
fixed manifest-only claim drained 30 `ubm-continuation-0` values with
`droppedItems: 0`, `droppedBytes: 0`, `controlLost: 0`, `disposed: true`. Logcat
contained no app ANR during the run. Retained private receipts:
`2026-09-22-f242-physical-wake-logcat.txt`, `2026-09-22-f242-status.jsonl`, and
`2026-09-22-f242-backlog-success.jsonl`.

**Test-design notes for whoever repeats this** (each cost time today):

- Android restarts a crashed FOREGROUND activity by itself. Send
  `input keyevent KEYCODE_HOME` before `am crash`, or the restart is the window
  manager, not a presence wake.
- CDM rebinds a bound service that dies roughly every ten seconds while the
  association is in the present state. Those rebinds are NOT appearance events
  and correctly produce no wake record; do not read them as "the strap is
  near".
- CDM only emits `onBleCompanionDeviceFound` on an absent-to-present
  transition. Walking out of range and back did not produce one for this
  association within ~10 minutes, while the co-located Galaxy Watch
  (`associationId( 1 )`) did. Until the presence state actually goes absent,
  returning produces no new wake — so a walk is not a reliable trigger.
- `am force-stop` disables presence wake entirely. Only `am crash` / `am kill`.

## 2026-09-21 night — four slices on the CI failures at c3d85af4

- 242 FIXED / PHYSICAL (initial fix `bc3eb797`, final closure above): two wake-context
  defects, both absent in the foreground. The wake connected `when-available`
  (leaning toward Android autoConnect: a slow, fragile link, pointless because
  presence only fires once the peer is advertising); and discovery carried no
  budget while the executor's own 10 s latch contradicted the core's 120 s
  window — so `gatt.discover timed out after 10000ms` was the executor's timer
  reported as `platform.failure`, which the persisted wake record read back
  from the phone confirms. Now `direct`, a 20 s discovery budget, and every
  latch longer than the core's budget. Four RED-proven tests; Android 382/382.
  The service now admits each callback to one serial worker and returns to CDM
  before the bounded continuation work starts; a RED/GREEN regression holds a
  native continuation and proves the callback has returned, while service
  teardown drains admitted work without interruption. `execute()` and
  the prepare/acknowledge claim path also reserves the session atomically before either does
  radio I/O, so a foreground claim cannot dispose an executing wake (and the
  reverse order produces a typed lifecycle refusal rather than starting a
  second operation). The physical closure, occurrence conversion, manifest
  claim authority and no-ANR observation are recorded in the finding above.
- 243 FIXED (`d69a64d8`): the packed Electron smoke's stand-in main offered IPC
  protocol 3 after 5773b3a6 bumped `IPC_PROTOCOL_VERSION` to 4. The check had
  never passed at 4; a test now pins the fixture to the constant.
- 244 FIXED (causal ordering replacement): a desktop reset now carries the
  core-observed power and the sequence of that fact. TypeScript advances the
  generation with that causal state and ignores queued adapter observations at
  or before the sequence; later recovery observations remain visible at the new
  generation. Regression coverage holds a delayed state queue, rapid off/on,
  stale pre-reset state, and direct removal. This replaces the prior
  status-read inference, which could suppress a reset based on an unrelated
  snapshot.
- 245 FIXED (`49316661`): the React Native Android expired-deadline test spanned
  three clock domains; now deterministic with one frozen clock, bound
  unchanged.
- 246 FIXED (TCK-only): the desktop WinRT S5 slow-drain acceptance raced the
  independent desktop notification pump.
  `flush()` now acts as a deterministic synthetic ingress barrier before the
  capacity-four slow-drain reads begin; the production stream accounting is
  unchanged. Linux and Windows exercise the same overflow/conservation shape.
- 247 FIXED (Windows JS leg, reproduced on a real Windows VM): Git Bash could
  resolve a copied Python shim under a host temp directory but not execute it.
  Test shims now live under Git Bash's executable `/tmp`; the production TV
  stage uses a temporary tar archive rather than a tar pipe, which also avoids
  macOS system-tar write failures. Focused staging/fallback tests passed on the
  clean Windows Node 22 checkout and macOS.
- 248 FIXED (release provenance): Cargo's legacy slash-separated declarations
  are alternatives and now generate parenthesized SPDX `OR` expressions.
  `btleplug@0.12.0` is the one reviewed exception: its vendored `LICENSE.md`,
  pinned by SHA-256 `95f1ea7e261c12c46fe8f67d2ddb7a92ebb1a5fd10d127e4ab3003f0701d9f56`,
  establishes `BSD-3-Clause AND (MIT OR Apache-2.0)`. Regenerated SBOM and
  third-party license output record zero unresolved Cargo entries.
- 249 FIXED (Android release builder, macOS host): the JNI export check used
  the host `nm -D` to inspect an Android ELF library. macOS `nm` has no GNU
  `-D` mode, so it falsely reported present JNI symbols as missing after a
  successful build. The builder now uses the pinned NDK's `llvm-nm` on every
  host; both committed release ABIs rebuilt, passed the export check and passed
  the 16 KiB ELF alignment check.
- 250 FIXED (desktop native builder): `build-napi-addon.js` passed
  `CARGO_TARGET_DIR` through to Cargo but always copied its output from the
  default `target/` tree, so an isolated build could install a stale binary.
  Output discovery now resolves the same absolute or root-relative target tree
  Cargo used, with a focused regression for all three forms.
- 251 FIXED (pinned Rust toolchain): `rustup run <pin> cargo` could still launch
  an older Homebrew `rustc` earlier on `PATH`, despite the builders reporting
  the pinned toolchain. Both Android and desktop N-API builders now resolve and
  pass the pinned toolchain's exact `rustc`; fresh isolated-target release
  builds succeeded with Rust 1.98.1 while the host `rustc` remained 1.97.1.

## External release review at `50642fa` — closure

The review bundle `unified-ble-manager-50642fa-review-and-repros.zip` was
verified against its source manifest and addressed as one release gate:

- **R1 fixed:** a repeated continuation wake reconciles the Rust owner's live
  connection, database generation and subscription routes before returning an
  idempotent success. A stale link or route reconnects; existing backlog and
  selector identity remain owned by the same session.
- **R2 fixed:** Rust seals the outbox under the same mutex as data admission.
  Kotlin retains each drained claim behind an opaque token until TypeScript has
  decoded it. Cleanup starts only after acknowledgement, and native intake
  observed after the cutoff is reported through `afterCutoffLoss`. A rejected or
  malformed acknowledgement returns the decoded backlog with explicit disposal
  uncertainty; native keeps one empty terminal receipt so a retry can confirm
  cleanup without delivering the values again.
- **R3 fixed:** claims carry the immutable session selector map. Same-count,
  reordered and occurrence-changed declarations cannot silently reuse or
  relabel an active continuation.
- **R4 fixed:** Kotlin validates the complete drain-batch root before treating
  it as complete. A valid prefix followed by a malformed or capped tail is
  replayable until decoded, advances only after acknowledgement, reports its
  explicit failure and never authorizes session disposal.
- **R5 fixed:** both the desktop duplicate-ticket cache and released terminal
  core scan history are bounded to 256 entries. Scan operation ids carry an
  attachment-scoped authenticated scan identity, preserving recent duplicate
  cancellation, explicit expiry, foreign ownership rejection and
  `argument.invalid` for forged non-scan ids without retaining an unbounded
  issued-id set.
- **G1 and G2 fixed before this review pass:** the adapter-reset test now waits
  for the native causal reset rather than assuming held receivers are a
  processing fence. Windows uses executable Git Bash Python fixtures and real
  absolute path expectations. The follow-up push run completed all 20 jobs
  successfully; the corresponding PR run's Linux Rust job was later cancelled
  while new review remediation was in progress, after every other completed
  lane had passed.
