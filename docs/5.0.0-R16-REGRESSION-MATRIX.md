<!-- docs/5.0.0-R16-REGRESSION-MATRIX.md -->

# UBM 5.0 R16 regression matrix

**Finding:** PR210 R16 (BLOCKER for qualification) — acceptance-layer
honesty: relabel synthetic legs, prove real bindings where the seam allows,
and disposition the required regression matrix with evidence, not intent.

**Scope note (packet G):** this matrix is authored on the solo 5.0.0 lane;
independent adversarial review is still pending. Every row states what was
real and what was mocked. Synthetic-seam passes never close physical,
background-lifetime, or radio gates.

**Evidence tiers used below:**

- `DEVICE` — executed on the emulator against the installed native module
  (no injected binding).
- `PACKED` — executed against the packed candidate (tarball/addon built
  from packed sources).
- `SEAM` — jest against a fake native binding (injected at the documented
  test seam; proves JS routing/ownership, not the native side).
- `CONTRACT` — unit/TCK proof of a contract primitive (parser, state
  machine, vector).
- `BLOCKED` — requires R03 core ops (connect/gatt/subscribe/counters) or a
  physical radio; sequenced, not waived.
- `OPEN` — seam-testable now; test not yet written.

## Matrix (PR210 required rows, verbatim)

| # | Test | Verdict | Evidence |
|---|------|---------|----------|
| 1 | Ordinary 5.x factory route — no test-binding option; real installed native host; legacy TS core/provider poisoned; BLE operations reach candidate Rust. | `DEVICE` + `SEAM` PROVEN | `R01FACTORY` 14/14 (`emulator-probe/results/battery-r01factory.json`: public no-options factory creates + concrete adapter read + destroys through `UnifiedBleRustCore`); `r01-poison` 5/5 (attach + legacy entries poisoned, create/scan/cancel/destroy reach the binding); `r01-factory-flip` 5/5; T8 14/14 (`battery-t8-postflip.json`: pre-aborted find settles `operation.aborted` on device, live core scan settles `operation.aborted` via the 1.5 s abort — provider↔core scan interop fixed: frozen arg shapes, DATA-02 integer millis, tolerant `op_id` read). |
| 2 | Missing/foreign core — binding missing or revision mismatched; no BLE side effect; admission ownership cleaned up. | `SEAM` PROVEN | `r01-poison`: missing → `capability.unsupported` with no session opened; foreign revision → `protocol.incompatible` with `native.close` observed. Leg G `rn-revision-gate` (`PACKED`, synthetic seam). |
| 3 | Native rejection — permission/ownership/capability denial in the core; zero associated OS commands. | PARTIAL | Router mints `denied`/`unsupported` pre-BLE: `RustCoreAdapterStateReader` returns before touching the adapter when permissions are absent (code). A device denial-path run (perms denied) is still open. |
| 4 | Concurrent client ownership — two clients sharing one host; one disposal does not erase the other's links/streams. | `SEAM` PROVEN (links/streams → R03) | `d2v-entrypoint-ownership`: RN + Expo managers own isolated sessions with paired closes. Physical-link/stream sharing needs connect/subscribe core ops. |
| 5 | Pre-abort and expired deadline — reject before radio effects at the native/provider boundary and public route. | `SEAM` PROVEN | `d2v-entrypoint-ownership`: pre-aborted signal → `operation.aborted`, expired deadline → `operation.timed-out`, zero new native ops. Check lives in the internal manager both routes share. |
| 6 | Cancellation mapping — two concurrent operations; cancel one; only its exact admitted native operation transitions. | PARTIAL | `r01-poison`: abort during in-flight connect emits `op.cancel` with the exact minted `operationId`. The two-concurrent-ops variant is open. |
| 7 | Dispatched write timeout — caller outcome uncertain as required; no automatic replay; later physical completion/cleanup accounted for. | `BLOCKED` (R03) | No `gatt.write` core op exists on any mobile binding; timeout enforcement is core-side. Provider passes `timeoutMs` through (code). |
| 8 | Admission failure cleanup — foreign revision, throwing revision method, failed bootstrap; no session leak; close failures observable. | PARTIAL | Foreign revision + session close proven (`r01-poison`, Leg G `rn-revision-gate`). Throwing `contractRevision` and close-failure observability are open. |
| 9 | Cleanup negative acknowledgement — native resolves release-failed; JS must not report released. | `BLOCKED` (contract + R03) | The core emits the NACK (`drive_destroy` → `released`/`release-failed`) but no binding wires a failure identity: the frozen vocab has no cleanup-failure code, NAPI has no dispose mapping, and the router reports `released`. Needs a contract decision, not a local tweak. |
| 10 | Failed unsubscribe retry — first disable fails; retained context allows real retry; no second-call synthetic success. | `BLOCKED` (R03) | No subscribe core op on device. Happy-path subscribe/unsubscribe routing is `SEAM`-proven (`r01-native-manager`). |
| 11 | Scan admission/stop race — hold OS start; request stop; complete start; no orphan active scan. | `OPEN` (seam-testable) | The fake binding supports deferred `scan.start` (pattern proven by the poison cancel test); the race test is not yet written. |
| 12 | Scan start/shutdown/drop — hold start; shutdown or drop caller; late start compensated; no resurrected owner. | `OPEN` (seam-testable) | Same deferred-fake pattern; not yet written. |
| 13 | Scan stop failure/hang — retain ticket; bounded truthful outcome; retry invokes actual stop. | `OPEN` (seam-testable) | Same deferred-fake pattern; not yet written. |
| 14 | Stale handles — cross-manager, prior connection generation, prior discovery generation; fail before I/O. | `CONTRACT` PROVEN | `ContractInvariants`: rejects stale paths, crossed attachment/generation identity, and cross-generation rebinding before dispatch. TCK `ubm5-race-bounds-cleanup` + `production-tck` cover stale races. Provider wiring rides the routing tests. |
| 15 | Lifecycle invalidation — services-changed/reset/disconnect/revocation reach correct typed consumers and invalidate only correct scopes. | PARTIAL | `r01-native-manager`: `connected`/`requested-disconnect` projection with exact causes/sequences. `services-changed`/reset/revocation need core events (`events.take` is empty pre-R03). |
| 16 | Descriptor parity — duplicate services/characteristics/descriptors and occurrences; exact intended instance addressed. | `PACKED` PROVEN (synthetic seam) | Leg G `rn-descriptor-parity`: duplicate CCCD occurrences staged → reconstructed → addressed by occurrence (hole fixed, negative-controlled: restores to 0 descriptors with the old `continue`). Physical discovery → R03. |
| 17 | Notification lifecycle — values, indication identity where exposed, overflow, invalidation, closed; terminal reason preserved. | PARTIAL | Leg G `rn-notifies` (staged value round-trip); TCK deterministic overflow vector (`deterministic-tck-subscription-overflow`, run by `deterministic-r12-limits-conformance` + `ubm5-race-bounds-cleanup`). Physical-burst overflow stays a physical gate (battery T11 boundary). |
| 18 | Metadata parity — name/services/manufacturer/service data; distinguish absent/unknown/empty; byte-exact payloads. | PARTIAL | Staged advertisement mapping end-to-end (Leg G `rn-scan-observes`; F01 binding shapes manufacturer/service-data). Byte-exact physical-beacon proof stays a physical gate. |
| 19 | Non-Node boundary — no Buffer global; supported wire forms decode correctly or fail structurally, not ReferenceError. | `SEAM` PROVEN | `r16-non-node-boundary` 3/3: array form decodes without Buffer; base64 form fails `protocol.malformed` without Buffer (fixed `bytesFromCore`, was ReferenceError); base64 decodes with Buffer (control). |
| 20 | Malformed values — invalid bytes, floats/NaN/infinity/negative occurrences, invalid enums and oversize inputs rejected without coercion/effects. | PARTIAL | `ContractInvariants` (malformed versions, byte limits, capability lies, stale/generation rejection); provider strict parsers fail `protocol.malformed` (code). Dedicated occurrence/enum fuzz is open. |
| 21 | Resource endurance — repeated manager/watch/scan/connect/subscription cycles return native and bridge residues to baseline. | `BLOCKED` (R03) | Router `counters.describe` is honestly static (zeros); `live_operations`/`retained_cleanup` exist in the core but no JNI op moves them (scan runs on the staged core — code-verified), so no dynamic counter is observable pre-R03. |
| 22 | Idle behavior — measure no-work wakeups/bridge calls; no unexplained 5 ms polling per idle channel. | DOCUMENTED (known cost) | The backend events pump polls `events.take` every 5 ms for the whole backend life (code: `pumpDelay`, unref'd). Explained (delivery pacing, mirrors the NAPI proof loop), not yet measured on device; backoff/push is a physical-gate follow-up. |

## Relabeled synthetic legs (kept, honestly named)

- `scripts/ci/f01-factory-routing-proof.cjs` (Leg G): header now declares
  REAL (packed `lib` factory code, packed NAPI addon executing
  DesktopCentral/ubm-core policy, frozen revision, throwing control proxy)
  vs SYNTHETIC (stubbed react-native, injected `rustCore` binding,
  hardcoded adapter/counters, fabricated scan IDs, `not-cancellable`
  cancellation, staged radio, stub Tauri transport). Real-binding
  counterpart: `R01FACTORY` emulator leg.
- `scripts/ci/f01-packed-dispatch-proof.js`: legs A–E/G–J are real
  (pack/install/pinned-toolchain build/identity/compiles/ELF); leg F runs
  the real packed addon with staged inputs (see the hardware-boundary
  declaration in `bindings/napi/js/dispatch_roundtrip.cjs`, which prints
  the branch taken).
- Battery boundary legs (T11/T12-style) keep their `boundary` verdicts
  with reasons; they never report synthetic passes as device receipts.

## Sequenced follow-ups (not waived)

1. R03 core ops (`connection.*`, `gatt.*`, `peers.*`, dynamic counters,
   core events): unblocks rows 7, 10, 15-remainder, 21 and the physical
   halves of rows 4, 16, 17, 18. Rerun the 8-verb matrix then.
2. R02-Apple Swift creation-surface mirror + simulator proof: the iOS
   facade still fails creation closed (no `adapter.state` case).
3. Seam tests rows 6-remainder, 8-remainder, 11, 12, 13, 20-fuzz.
4. Contract decision for the cleanup-NACK failure identity (row 9).
5. Physical gates: radio discovery, burst overflow, idle-rate
   measurement, background lifetimes (packet G rule: synthetic passes
   never auto-close these).
