# docs/superpowers/

Every plan and design spec in this directory is a **historical record** of
work that has since been delivered (or superseded). File paths, API names, and
sequencing described here reflect the moment the plan was written, not the
current tree — do not implement from these documents. For live documentation,
start at the [documentation map](../README.md).
