# UBM 5.0 gate ledger (trackourhealth/bun-mono#1188)

Branch `5.0.0`. Every gate needs owner, scope, source SHA, artifact identity,
actual command, result/evidence, and remaining limitations. Local commits only —
no push past the license/provenance hold, no merge to `main`, no tags, no publish.

| Gate | Owner | State | Evidence / commands | Limitations |
|---|---|---|---|---|
| U-LICENSE | ulicense slice | ADOPTED-LOCAL | First new-license commit `3a081c08`, merged `bd7ea857`. SAL byte-exact; CT verbatim modulo wrapping. Gates green (209 suites/2205 at adoption; merged-tree License 25/25 + docs + artifacts green). Secrecy audits clean (lane-wide + emu evidence) | Legal/provenance review pending; public pushes held |
| U0 Baseline/isolation | orchestrator | COMPLETE | Baseline manifest + env record (preflight); CI fix `d8d7f4ae`; lane isolated, local-only | — |
| U1 Contracts/DRY | contracts + fix slices | ACCEPTED | C-UBM.0.1.1-DRAFT (`b1257846` + `04852327`): 165/165 TS + 60/60→128/128 Rust mirror tests; credible RED; U11 review + re-review verified all 14 rulings. TCK seam `7eeda38c` + fixes `3f48221f`: 29 scenarios deep-compared, packaging proof, pin enforced; root 2247/2247 green | Budgets await radio-qualified measurements |
| U2 Ownership/lifecycle | central slices | PARTIAL | Kernel + central merged (`f910b548` + `2ed0baea`): leases/generations/cancel/cleanup/retained-release, borrower/lease/transfer/revocation, success+failure release paths; 128/128 ubm-core tests; TCK seam vectors | Adapter-reset + host-lifetime scenarios need real adapters/hosts |
| U3 Operations/GATT | central slices | PARTIAL | GATT-01..04 in central (occurrence/ambiguity, service-change invalidation, one-owner CCCD + borrower survival, pre-ready quarantine) + TCK vectors; long-write planner, max-write, cancel boundaries | Dispatched-write-timeout + queued-expiry need host/radio-backed runs |
| U4 Streams/resources | kernel slices | PARTIAL | Bounded item/byte/aggregate budgets, reserved control, StreamSet, overflow-terminal tests (STR-01/02) | Load/perf validation pending measured budgets |
| U5 Platform lifetimes | TBD | NOT STARTED | Emulator battery on 4.x baseline merged (`da4a266e` + `30021956`): 14 tests, 57/57, 13 pass + 1 boundary — retained-behavior baseline only | 5.0-artifact battery needs adapters; Apple/Windows need other hosts |
| U6 FFI/security | TBD | NOT STARTED | Enablers: per-binding LIFETIME_RULES + panic containment proven on real hosts (`650d8ec4` + `4576ff27`); uniform post-close semantics; fuzz-ready seams | Parser/fuzz cases + Miri/sanitizers pending |
| U7 Native/core equivalence | TBD | NOT STARTED | Enablers: wired bindings, TCK seam, central; wasm32 ubm-core check green | Needs deterministic-backend R12 conformance pass + shared-corpus runs |
| U8 Packages/builds | TBD | NOT STARTED | Per-slice gates green (root 2247/2247; workspace 177 + 10 central-fix tests composed; contracts 165; harness 17/17) | Full final-tree rerun pending (fmt/check/clippy/test matrix + pack smokes) |
| U9 Fault/restart/migration | TBD | NOT STARTED | Planned: recovery + migration rehearsal on copied stores | Downstream DB recovery is external |
| U10 Physical/performance | TBD | PARTIAL | Emulator battery (4.x baseline) green; Linux host has NO BLE controller — physical rows need other hosts/rig | Hardware blocked; budgets unset; 5.0-artifact runs pending |
| U11 Review | orchestrator | PARTIAL | 6 rounds done (contracts, contract-fix re-review, wiring, TCK, central, emu), all verdicts dispositioned with fixes merged | Per-slice rounds continue |
| U12 Candidate handoff | orchestrator | NOT STARTED | Clean pushed `5.0.0` + packed identities + mapping | Blocked on above + hold-lift |

Environment sub-rows: U-LINUX RECORDED; U-ANDROID-EMULATOR BASELINE-GREEN (4.x;
5.0-artifact rerun pending adapters); U-ANDROID-PHYSICAL / U-APPLE / U-WINDOWS open.

## Open follow-ups (orchestrator-owned)

1. Owner legal/provenance review of adopted texts → publication hold-lift.
2. Deterministic-backend R12 limit conformance (pre-existing 8/32-byte subscribes) for U7 corpus parity.
3. HOST-WEB AbortSignal wrapper over the wasm stream protocol.
4. Android ART/ABI matrix + supported-ABI list; Swift/Kotlin consumer compiles (U-APPLE).
5. jni throw-semantics re-check + uniffi checksum re-check on version bumps.
6. `example/android` crash-loop (eager applicationContext NPE) → main-line triage, not a 5.0-lane fix.
7. Battery-v2: correct-PID maps capture + committed `unzip -l` named-.so proof.
8. Physical hardware + Apple/Windows hosts for U10/U5 evidence.
