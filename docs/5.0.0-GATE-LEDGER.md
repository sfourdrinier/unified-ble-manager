# UBM 5.0 gate ledger (trackourhealth/bun-mono#1188)

This ledger retains the U0–U12 receipts at their cited historical source SHAs.
It is not the authority for the live branch, push, tag, or npm-publication
state: the `5.0.0` branch has advanced past several listed snapshots.
[`../RELEASE.md`](../RELEASE.md) and `.github/workflows/publish.yml` are
canonical for publication. An initial `5.0.0-rc.1` publication requires an
annotated tag on the exact current `main` commit; the workflow assembles the
complete native prebuild matrix, stages and verifies RustCore, publishes with
npm OIDC/provenance, then binds the registry tarball and provenance to that
tag. Until that procedure succeeds, no row below is an npm-publication claim.

| Gate | Owner | State | Evidence / commands | Limitations |
|---|---|---|---|---|
| U-LICENSE | ulicense slice | ADOPTED-LOCAL | First new-license commit `3a081c08`, merged `bd7ea857`. SAL byte-exact; CT verbatim modulo wrapping. Gates green (209 suites/2205 at adoption; merged-tree License 25/25 + docs + artifacts green). Secrecy audits clean (lane-wide + emu evidence). Owner authorized the lane-branch push | Legal/provenance review pending; merge/tags/publish still held |
| U0 Baseline/isolation | orchestrator | COMPLETE | Baseline manifest + env record (preflight); CI fix `d8d7f4ae`; lane isolated, local-only | — |
| U1 Contracts/DRY | contracts + fix slices | ACCEPTED | C-UBM.0.1.2-DRAFT (profile.codec frozen `2428f4e8`): 171/171 TS + Rust mirror tests; U11 review + re-review verified all 14 rulings. TCK seam `7eeda38c` + fixes `3f48221f`: 29 scenarios deep-compared, packaging proof, pin enforced | Budgets await radio-qualified measurements |
| U2 Ownership/lifecycle | central slices | PARTIAL | Kernel + central merged (`f910b548` + `2ed0baea`): leases/generations/cancel/cleanup/retained-release, borrower/lease/transfer/revocation, success+failure release paths; 128/128 ubm-core tests; TCK seam vectors | Adapter-reset + host-lifetime scenarios need real adapters/hosts |
| U3 Operations/GATT | central slices | PARTIAL | GATT-01..04 in central (occurrence/ambiguity, service-change invalidation, one-owner CCCD + borrower survival, pre-ready quarantine) + TCK vectors; long-write planner, max-write, cancel boundaries | Dispatched-write-timeout + queued-expiry need host/radio-backed runs |
| U4 Streams/resources | kernel slices | PARTIAL | Bounded item/byte/aggregate budgets, reserved control, StreamSet, overflow-terminal tests (STR-01/02) | Load/perf validation pending measured budgets |
| U5 Platform lifetimes | host-android slice | PARTIAL | five0 5.0-artifact battery merged (`6362e909`): 11 records, 49/49 assertions on x86_64 API-34 AVD (REAL-EMULATOR + SIMULATED-injection labeled); 4.x baseline 57/57 retained | No BLE peers on emulator; Apple/Windows need other hosts |
| U6 FFI/security | ffi + host-android slices | PARTIAL | LIFETIME_RULES + panic containment on real hosts (`650d8ec4`); JNI GATT bridge: 26/26 Rust tests + 44-check JVM exchange + 11/11 Kotlin HOST-JVM; uniform post-close semantics; merged `bf6d7c8b`: Miri-clean 6 crates, `unsafe` net −7, cargo-audit + cargo-deny 0, 6 libFuzzer targets ×1M runs 0 crashes | Sanitizer (asan/tsan) passes not run |
| U7 Native/core equivalence | u7 + dbackend slices | PARTIAL | Wired bindings + TCK seam + R12 dbackend conformance (`fada1201`); parity 48/48; wasm32 ubm-core check green | Full shared-corpus runs pending |
| U8 Packages/builds | orchestrator | HISTORICAL COMPLETE (Linux) | At `2003024f`: root 2273/2273 + parity 48/48 (split: parity suites need the napi addon, routed to the dedicated config/job), cargo 16 suites, both fmt scopes, clippy, tauri 75/75, lint, contracts 171/171, plugin 45/45, docs, artifacts, prepack, web build, perf, electron smoke, all pack proofs, classic RN assemble, Expo CNG assemble; `rust-5-0` (Linux/Win/Mac matrix) + `rust-parity-5-0` CI jobs in tree (`bd73e77d`); expo-doctor drift fixed in lane (`47b8704b`, 21/21); example crash-loop NPE fixed in lane (`ef514c97`, emulator-verified); hosted suite green at `45587976` (PR run `35062952377` + push run `35062949582`). Current publish behavior additionally requires the tag workflow's complete native prebuild matrix, RustCore staging, source-identity checks, canonical tarball checks, and registry/provenance binding. | Historical receipt only; tvOS was library-check only. Re-run release gates from the exact tag commit; publication is decided and performed only by `RELEASE.md` + `publish.yml`. |
| U9 Fault/restart/migration | u9 slice | COMPLETE | u9 battery 6/6 gates + 24/24 invariants on API 33 + 35 x86_64 AVDs (real-emulator reboot polling, cold-boot recovery, registry + wake-path, fault drill); migration no-op pinned (`DATA_VERSION`) + rehearsed on copied stores | Downstream DB recovery is external |
| U10 Physical/performance | TBD | PARTIAL | Emulator batteries green (4.x 57/57 + five0 49/49); Linux host has NO BLE controller — physical rows need other hosts/rig | Hardware blocked; budgets unset |
| U11 Review | orchestrator | PARTIAL | 12 rounds done (contracts, contract-fix re-review, wiring, TCK, central, emu, staged, desktop, host-android 3-way, u9-packet, fuzz-slice, hosted-fix), all verdicts dispositioned with fixes merged | Per-slice rounds continue |
| U12 Candidate handoff | orchestrator | HISTORICAL READY-FOR-REVIEW | Handoff packet `docs/5.0.0-U12-HANDOFF.md` at `2003024f` + docs delta: packed sha256, gate mapping, reproduction, blockers | Historical handoff state only. Its listed checks and publication discussion do not describe the current tip or authorize tagging/publishing. |

Environment sub-rows: U-LINUX READY; U-ANDROID-EMULATOR READY (5.0-artifact
five0 49/49); U-ANDROID-PHYSICAL / U-APPLE / U-WINDOWS BLOCKED (no hardware/hosts).

## Historical follow-ups (orchestrator-owned)

The list below is preserved with its contemporaneous disposition. Re-evaluate
each item against the current branch and release procedure; it is not a live
release checklist.

1. Owner legal/provenance review of adopted texts → publication hold-lift.
2. ~~Deterministic-backend R12 limit conformance~~ DONE (`fada1201`).
3. HOST-WEB AbortSignal wrapper over the wasm stream protocol.
4. Android ART/ABI matrix + supported-ABI list; Swift/Kotlin consumer compiles (U-APPLE).
5. jni throw-semantics re-check + uniffi checksum re-check on version bumps.
6. ~~`example/android` crash-loop (eager applicationContext NPE)~~ DONE in lane (`ef514c97`, emulator-verified).
7. Battery-v2: correct-PID maps capture + committed `unzip -l` named-.so proof.
8. Physical hardware + Apple/Windows hosts for U10/U5 evidence.
9. ~~Kotlin bridge non-blockers~~ DONE in lane (`aa41672b`, tests green).
10. ~~expo-doctor drift (3 checks)~~ DONE in lane (`47b8704b`, 21/21 + chain green).
11. ~~Push `5.0.0` + draft PR + hosted green~~ DONE (pushed `45587976`,
    draft PR #210 + `ci:apple`; 3 triage rounds, 5 lane fixes; full
    hosted suite green incl. Win/Mac + Apple).
12. ~~R02-Apple Swift creation-surface mirror~~ CODE DONE
    (`b6d3def4` + `@nonobjc` fix `808b9cd1`: live-central
    `adapter.state`, zero counters, empty events, honest cancel,
    destroy+released; counters byte-identical to Android; full matrix
    green on tip — push run `35299312179` 18/18 incl. both iOS
    examples + tvOS, lane run `35299309187` success).
    Simulator proof still pending (needs macOS host).
13. ~~Live-notify triumvirate (exit panic + evap mid-subscribe)~~ CODE DONE
    (`dc8d7afa`: dispatch open rides `desktop_runtime()`; `BtleplugRadio::drop`
    enter-guarded inline; bluez+RN pump timers ref'd; regression test fails
    on unref'd code, passes on fixed). Live Polar H10 E997042F proof:
    scan/connect/7 services/battery 90%/16x85bpm HR/unsubscribe/disconnect
    1910ms/receipt, exit 0, no panic. Gates: cargo ubm-desktop+napi 0
    failed, eslint+tsc clean, jest bluez 16/16 + RN 31/31. CI verdicts on
    tip `177f7941` (fmt-fix requeue): FULL MATRIX GREEN — push run
    `35308091048` success (16/16 incl. contracts, napi parity, Rust
    workspace x3, Tauri x3, JS x4, Expo CNG Android, Classic RN) + lane
    run `35308093065` success (18/18 incl. both iOS examples + tvOS).
    (Prior `35307036662`/`35307039510` superseded by the fmt fix.)
