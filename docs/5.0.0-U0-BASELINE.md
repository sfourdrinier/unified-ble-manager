# UBM 5.0 U0 baseline manifest

Status: U0 baseline inventory recorded. Branch `5.0.0`, base `8c8195dd0430ff847d9492ce32f4e63ea3a5df1d`
(`fix(ios): own restoration radio across module lifetimes (#208)`).
Provenance: read-only inventory of this lane at base, reviewed by orchestrator
(trackourhealth/bun-mono#1188). `5.0 destination` stays `TBD-U1` until the U1
contract freeze; environment rows await the Linux preflight agent
(`PENDING-PREFLIGHT`). Nothing here claims 5.0 is released.

Maintenance watch: `git fetch origin` on 2026-09-15 observed new tag `v4.0.28`.
Lane integration of any main-side maintenance stays deliberate with rerun evidence.

## (a) Public entrypoints (export map, `package.json:296-563`)

| Row | Export | Contract essence | Current evidence | 5.0 dest | Verification |
|---|---|---|---|---|---|
| E-01 | `.` root | Host-neutral, never chooses a radio (`src/index.ts:1-6`) | No Preview/Supported claim (`docs/generated/PLATFORM_SUPPORT.md:9-11`) | TBD-U1 | `pnpm prepack && pnpm typecheck`; `pnpm docs:check` |
| E-02 | `./backend-sdk` | Backend-authoring contract + TCK runner (`src/backend-sdk.ts`) | Package-level only | TBD-U1 | `pnpm test:package`; `pnpm docs:backend-sdk` |
| E-03 | `./cli` | `doctor/capabilities/trace/tck/scenario/inspect/init/support-bundle` | `compile-config-loadability` only (`docs/CLI.md:26`) | TBD-U1 | `pnpm test:package`; `node bin/ubm.js doctor` |
| E-04 | `./testing` | Deterministic backend/manager/virtual-clock (test-only) | Explicitly not radio proof (`src/testing.ts:5-7`) | TBD-U1 | `pnpm test:package` |
| E-05 | `./codecs` | Binary-only primitives + ieee-11073 | Package-level only | TBD-U1 | `pnpm test:package`; `pnpm typecheck` |
| E-06..E-13 | `./profiles/*` | commands, standard-commands, heart-rate, battery-service, device-information, health-thermometer, blood-pressure, ieee-11073 | Guide `docs/PROFILES_AND_COMMANDS.md`; package-level evidence | TBD-U1 | `pnpm test:package` |
| E-14 | `./web` | `createWebBleManager`; chooser/session, `scan()` rejects | No live claim; absent from validated table | TBD-U1 | `pnpm build:example:web`; `pnpm typecheck` |
| E-15 | `./react-native` | `createReactNativeBleManager`; TurboModule control + Expo runtime | Historical L0/blocked only | TBD-U1 | PENDING-PREFLIGHT + `build/test:android`, `build/test:ios`, `test:native-protocol:android/:apple` |
| E-16 | `./react` | Hooks (`useBle`, `useAdapterState`, …) | Package-level only | TBD-U1 | `pnpm test:package`; `pnpm lint` |
| E-17 | `./node/bluez` | `createBluezBleManager`; app-supplied `dbus-next` | `reported-unverified-linux-bluez-live@1`, L0/blocked, unbound | TBD-U1 | PENDING-PREFLIGHT (U-LINUX) |
| E-18 | `./node/corebluetooth` | `createCoreBluetoothBleManager` | Experimental L0/blocked, unbound | TBD-U1 | PENDING-PREFLIGHT (U-APPLE) |
| E-19 | `./node/winrt` | `createWinRtBleManager` | No validated record (absence is the finding) | TBD-U1 | PENDING-PREFLIGHT (U-WINDOWS) |
| E-20 | `./electron/main` | Main owns radio; node boundaries | Prebuild-backed; ABI load is not a live claim | TBD-U1 | PENDING-PREFLIGHT (U-APPLE, U-WINDOWS) |
| E-21 | `./electron/renderer` | Versioned IPC client, never loads addon | Same as E-20 | TBD-U1 | `pnpm test:package` + E-20 preflight |
| E-22 | `./tauri` | `createTauriBleManager`; btleplug radio in Rust; ipcProtocol 2 | Crate NOT on crates.io (path dep) | TBD-U1 | `pnpm typecheck`; cargo build (toolchain PENDING-PREFLIGHT) |
| E-23 | `./advanced` | Maintainer/host-authoring facade | Package-level only | TBD-U1 | `pnpm typecheck`; `pnpm test:package` |
| E-24 | `./expo` | `createExpoBleManager`; Expo Go unsupported | No expo record in validated table | TBD-U1 | PENDING-PREFLIGHT + `test:expo`, `build:expo:android`, `test:plugin` |
| E-25 | `./app.plugin.js` + `./package.json` | Expo prebuild plugin passthrough | Build recipe, not evidence | TBD-U1 | `pnpm build:plugin`; `pnpm test:plugin` |

## (b) Native / host targets

| Row | Target | Evidence / note | Verification |
|---|---|---|---|
| T-01 | `ubm` bin (`bin/ubm.js`) | `docs/CLI.md:1-28` | `pnpm prepack && node bin/ubm.js doctor` |
| T-02 | Android library `com.sfourdrinier.unifiedblemanager` (minSdk 24, target/compile 36) | No validated record | PENDING-PREFLIGHT (U-ANDROID/EMULATOR) |
| T-03 | Apple pod (module `BlePlx`, iOS/tvOS 16.4, C++20) | tvOS characterization only; iOS L0/blocked | PENDING-PREFLIGHT (U-APPLE) |
| T-04 | Tauri crate `tauri-plugin-unified-ble-manager@4.0.0` (edition 2021, rust 1.77, btleplug 0.12) | Unpublished; compat frozen | PENDING-PREFLIGHT (cargo toolchain) |
| T-05..T-08 | Node-API v8 prebuilds (darwin-arm64/x64 corebluetooth, win32-arm64/x64 winrt) | Tagged-release artifacts; ABI load is not live proof | PENDING-PREFLIGHT (U-APPLE, U-WINDOWS) |
| T-09 | Versioned native protocol v2 (schema + ABI) | Fail-closed versioning | `pnpm native-protocol:check`; `pnpm test:native-protocol*` |

## (c) Runtime capabilities (truth = instantiated backend)

- C-01 `manager.capabilities.supports/get`; deterministic TCK L1 in `pnpm test:package`; live L4+ PENDING-PREFLIGHT.
- C-02 Android foreground-service lease (absent by default, `restart:'never'`, POST_NOTIFICATIONS gating). No validated record.
- C-03 Peer directory: `peers.bonded()/resolve()` RN-Android only; web partial; else `capability.unsupported`.
- C-04 RN notification ingress bounds (512 records / 1 MiB, `stream.overflow`).
- C-05 Deferred: Meta Quest + nRF52840 fault injection are NOT 4.0 entrypoints (confirm still deferred for 5.0).

## (d) Package / artifact identities

- npm `unified-ble-manager@4.0.28`; stamp `UNIFIED_BLE_IMPLEMENTATION_VERSION = '4.0.28'`.
- Crate `tauri-plugin-unified-ble-manager@4.0.0`; Tauri compat npm/crate `^4.0.0`, ipcProtocol 2.
- Addons `unified_ble_corebluetooth`, `unified_ble_winrt` (NAPI v8); pod module `BlePlx`; Android namespace above.
- Protocol v2 schema + ABI; bob targets commonjs + module + typescript; SBOM CycloneDX 1.6 + `THIRD_PARTY_LICENSES.json`.

## (e) Toolchain requirements (as recorded in repo)

- `packageManager pnpm@10.14.0`; node `^22.13.0 || ^24.3.0 || >=25.0.0` (`.nvmrc` 22.13.0); TS `^5.2.2`, Jest `^29.7.0`.
- Rust (Tauri crate only): edition 2021, `rust-version = "1.77"` — historical metadata, not the 5.0 toolchain.
- Android: AGP 8.8.0, kotlin 1.9.24 default / 2.0.21 BlePlx, NDK 27.1.x default vs 26.1.x BlePlx (consumer-context dependent).
- Installed tool versions: PENDING-PREFLIGHT.

## (f) Public / private boundary

This public lane holds: host-neutral JS API + entrypoints, first-party host implementations,
versioned protocol + codegen, Expo plugin, `ubm` CLI, TCK/testing harnesses, public docs,
examples, SBOM/license inventories, `evidence/v1/`. Excluded from published files
(`package.json:31-54`): `docs/superpowers`, `docs/audits`, `docs/review`, `docs/evidence/g0/**`,
V1 docs, `docs/index.html`, `docs/assets`, build/test internals. Forbidden in public
history: private vendors, health archive, engine algorithms, product policy, private disclosures.

## (g) Branch / base facts

- Lane worktree `/home/stephane/src/unified-ble-manager-5.0.0`, branch `5.0.0`, base = HEAD at
inventory time `8c8195dd0430ff847d9492ce32f4e63ea3a5df1d`, tree clean, tracking `origin/5.0.0`.
- Single remote `origin https://github.com/sfourdrinier/unified-ble-manager.git`.
- Worker branches `codex/ubm5-*` target `5.0.0`, never `main`. No pushes past the license hold.

## (h) Flags

- F-01: no live-radio label is bound to `4.0.28` (generated page says so explicitly).
- F-02: prebuild binaries are release-time artifacts, not lane-verifiable.
- F-03: registry/crates.io state is registry-side, not verified here.
- F-04: effective Android SDK/NDK values depend on consuming-app Gradle context.
- F-05: all PENDING-PREFLIGHT environment rows deliberately unsupplied here.
