<!-- android/src/main/AndroidManifestNew.xml -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <!-- AGP 8+ active manifest (build.gradle sourceSets.main.manifest.srcFile).
       Namespace is declared in android/build.gradle; keep package off this file. -->

  <uses-permission
    android:name="android.permission.BLUETOOTH"
    android:maxSdkVersion="30" />
  <uses-permission
    android:name="android.permission.BLUETOOTH_ADMIN"
    android:maxSdkVersion="30" />
  <uses-permission
    android:name="android.permission.BLUETOOTH_SCAN"
    android:usesPermissionFlags="neverForLocation"
    tools:targetApi="s" />
  <uses-permission
    android:name="android.permission.BLUETOOTH_CONNECT"
    tools:targetApi="s" />
  <!-- CompanionDeviceManager presence observation is the process-death wake
       route. This is the AGP 8+ manifest selected by android/build.gradle, so
       consumers inherit the permission from the artifact they actually build. -->
  <uses-permission
    android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE"
    tools:targetApi="s" />

  <application />
</manifest>
