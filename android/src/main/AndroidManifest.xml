<!-- android/src/main/AndroidManifest.xml -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.sfourdrinier.unifiedblemanager"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission
    android:name="android.permission.BLUETOOTH"
    android:maxSdkVersion="30" />
  <uses-permission
    android:name="android.permission.BLUETOOTH_ADMIN"
    android:maxSdkVersion="30" />
  <uses-permission
    android:name="android.permission.BLUETOOTH_SCAN"
    android:usesPermissionFlags="neverForLocation"
    tools:targetApi="s" />
  <uses-permission
    android:name="android.permission.BLUETOOTH_CONNECT"
    tools:targetApi="s" />
  <!-- Presence observation (CompanionDeviceManager.startObservingDevicePresence)
       is how an associated peer wakes the app after the process is gone; without
       this the owner answers permission.denied and cold-start restoration cannot
       be armed at all (finding 226). Install-time permission, granted with the
       companion association the user already approved. -->
  <uses-permission
    android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE"
    tools:targetApi="s" />

  <application />
</manifest>
