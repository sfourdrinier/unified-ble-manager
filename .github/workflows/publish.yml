# .github/workflows/publish.yml
# Publish the canonical unified-ble-manager package with npm Trusted Publishing
# (OIDC) and provenance.
#
# Prerequisites (one-time):
# 1. GitHub Environment named "npm" (optional: required reviewers).
# 2. npm Trusted Publisher for unified-ble-manager:
#    Organization/user: sfourdrinier
#    Repository:        unified-ble-manager
#    Workflow filename: publish.yml
#    Environment name:  npm
#    Allowed actions:   npm publish
#    https://www.npmjs.com/package/unified-ble-manager/access
#
# Release flow: merge release PR → manually tag vX.Y.Z on main → push tag →
# approve npm environment if required → this workflow publishes the package
# with provenance, then creates the GitHub Release. Tags stay manual. See RELEASE.md.
# 4.0.0-rc.* publishes to npm latest so a bare install gets the current 4.0 line.
#
# Stable SemVer describes the public package/API contract. Backend support labels
# remain evidence-derived and are not promoted merely because the package is stable.
#
# GA gates (Ubuntu): package/plugin/lint/prepack, electron Fake L1 smoke, host
# export resolution, classic RN Android assemble, Expo CNG prebuild+assemble,
# canonical npm pack/install smoke, artifact reproducibility and registry binding.
# Apple compile remains a pre-merge gate (ci:apple on the release PR / main path
# filter) and is not redundantly re-run here. Align with scripts/verify-release.sh.

name: Publish to npm

on:
  push:
    tags:
      - 'v*.*.*'

# A retag of the same version cancels the superseded run. A different tag
# (v4.0.0 vs v4.0.0-rc.0) is a different github.ref and is not cancelled.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  native-prebuild-plan:
    name: Plan native Node-API prebuilds
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - name: Checkout tag
        uses: actions/checkout@v7.0.0

      - name: Emit maintained prebuild matrix
        id: matrix
        shell: bash
        run: echo "matrix=$(node scripts/native-prebuilds/print-github-matrix.js)" >> "$GITHUB_OUTPUT"

  # Node-API v8 is ABI-stable across the maintained Node lines and modern
  # Electron. Build once per OS/CPU, then bundle all four binaries in npm.
  native-prebuild:
    name: Native prebuild (${{ matrix.platform }}-${{ matrix.arch }})
    needs: native-prebuild-plan
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.native-prebuild-plan.outputs.matrix) }}
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout tag
        uses: actions/checkout@v7.0.0

      - name: Setup Python for node-gyp
        uses: actions/setup-python@v6.0.0
        with:
          python-version: '3.12'

      - name: Setup JS package
        uses: ./.github/actions/setup-js-package
        with:
          prepack: 'false'
          node-version: '24'

      - name: Build and load maintained Node-API prebuild
        shell: bash
        run: pnpm native-prebuild:build -- --backend "${{ matrix.backend }}"

      - name: Build public package artifacts for Electron prebuild smoke
        run: pnpm prepack

      - name: Load the same prebuild under Electron main
        shell: bash
        run: ./node_modules/.bin/electron scripts/ci/electron-main-smoke.js

      - name: Upload path-preserving native prebuild
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifactName }}
          path: .native-prebuild-artifact
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 7

  publish:
    name: Publish canonical package
    needs: native-prebuild
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: write # GitHub Release after successful publish
      id-token: write # npm Trusted Publishing (OIDC)
    env:
      NODE_OPTIONS: --max-old-space-size=8192
      GRADLE_OPTS: -Dorg.gradle.jvmargs=-Xmx6g\ -XX:MaxMetaspaceSize=1g\ -Dfile.encoding=UTF-8

    steps:
      - name: Checkout tag
        uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0 # Stable tags must prove their commit is current main.

      # Trusted publishing: Node ≥22.14.0, npm ≥11.5.1.
      # npm's current GitHub Actions guidance requires an explicit registry URL;
      # npm then exchanges the job's OIDC identity during `npm publish`.
      # Do not provide NODE_AUTH_TOKEN; trusted publishing must not fall back to
      # a long-lived credential. https://docs.npmjs.com/trusted-publishers/
      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Ensure npm supports trusted publishing
        run: |
          # Pin npm 11.x (docs floor 11.5.1). Avoid npm@latest if its engines
          # move beyond the selected Node release line.
          npm install -g 'npm@^11.5.1'
          node --version
          npm --version
          test -n "${ACTIONS_ID_TOKEN_REQUEST_URL:-}" || {
            echo "ACTIONS_ID_TOKEN_REQUEST_URL missing — OIDC will not work"
            exit 1
          }
          test -n "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" || {
            echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN missing — OIDC will not work"
            exit 1
          }

      - name: Enable pnpm
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Assemble native prebuild matrix
        uses: actions/download-artifact@v8
        with:
          pattern: 'native-prebuild-*'
          path: .
          merge-multiple: true

      - name: Verify and bind complete native prebuild matrix
        run: pnpm native-prebuild:verify --require-all --write-manifest

      - name: Run package tests
        run: pnpm test:package

      - name: Validate current evidence records
        run: pnpm validate:evidence

      - name: Run plugin tests
        run: pnpm test:plugin

      - name: Lint and typecheck
        run: pnpm lint

      - name: Build package artifacts
        run: pnpm prepack

      - name: Build 4.0 Web Bluetooth public example
        run: pnpm build:example:web

      - name: Production performance benchmark gate (host-native + JS)
        run: pnpm performance:check

      - name: Reproducible SBOM and production-license audit
        run: pnpm release:artifacts:check

      - name: Electron Fake multi-device demo smoke (L1)
        run: node example-electron/smoke.js

      - name: Canonical host export resolve (L2 packaging)
        run: |
          set -euo pipefail
          node scripts/ci/check-host-exports.js

      - name: Setup Java (classic RN + Expo CNG Android assemble)
        uses: actions/setup-java@v5.5.0
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4.0.1

      - name: Install classic example dependencies
        run: pnpm --dir example install --no-frozen-lockfile

      - name: Assemble classic RN Android debug APK
        working-directory: example/android
        run: ./gradlew :app:assembleDebug --no-daemon --console=plain -PreactNativeArchitectures=arm64-v8a

      - name: Install Expo example dependencies
        run: pnpm --dir example-expo install --no-frozen-lockfile

      - name: Align Expo peer deps to installed SDK
        working-directory: example-expo
        run: npx expo install --fix

      - name: Typecheck Expo example
        run: pnpm --dir example-expo exec tsc --noEmit -p tsconfig.json

      - name: Run Expo Doctor
        working-directory: example-expo
        run: NODE_ENV=development npx expo-doctor

      - name: Prebuild Expo native projects
        working-directory: example-expo
        run: NODE_ENV=development npx expo prebuild --clean --no-install

      - name: Assemble Expo CNG Android debug APK
        working-directory: example-expo/android
        run: NODE_ENV=development ./gradlew :app:assembleDebug --no-daemon --console=plain

      - name: Canonical pack+install export smoke
        run: node scripts/ci/pack-install-smoke.js

      # Focused packed host proof; this consumes only the canonical tarball and
      # remains separate from the Node/Web-only G6A schema.
      - name: Packed Expo/Tauri consumer proof
        run: node scripts/ci/packed-host-consumer-check.js

      # Independent G6A packed-consumer proof; keep separate from generic smoke to avoid artifact collisions.
      - name: G6A packed consumer proof
        run: node scripts/ci/g6a-packed-consumer-proof.js

      - name: Generate the exact canonical npm publish tarball
        run: |
          set -euo pipefail
          rm -rf .release-package
          mkdir .release-package
          PACK_OUTPUT="$(npm pack --pack-destination .release-package)"
          TARBALL_NAME="$(printf '%s' "${PACK_OUTPUT}" | node -e '
            const fs = require("fs")
            const lines = fs.readFileSync(0, "utf8").split(/\r?\n/).map(line => line.trim()).filter(Boolean)
            if (lines.length === 0) {
              throw new Error("npm pack did not report a tarball filename")
            }
            process.stdout.write(lines[lines.length - 1])
          ')"
          EXPECTED_TARBALL="$(node -p "require('./package.json').name + '-' + require('./package.json').version + '.tgz'")"
          test "${TARBALL_NAME}" = "${EXPECTED_TARBALL}"
          shopt -s nullglob
          TARBALLS=(.release-package/*.tgz)
          test "${#TARBALLS[@]}" -eq 1
          test "${TARBALLS[0]}" = ".release-package/${EXPECTED_TARBALL}"
          PUBLISH_TARBALL=".release-package/${TARBALL_NAME}"
          test -f "${PUBLISH_TARBALL}"
          echo "PUBLISH_TARBALL=${PUBLISH_TARBALL}" >> "$GITHUB_ENV"

      - name: Classify and guard canonical package release channel
        id: release_channel
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME#v}"
          ROOT_VER="$(node -p "require('./package.json').version")"
          ROOT_NAME="$(node -p "require('./package.json').name")"
          if [ "$TAG" != "$ROOT_VER" ]; then
            echo "Tag v${TAG} does not match root package.json version ${ROOT_VER}"
            exit 1
          fi
          if [ "$ROOT_NAME" != "unified-ble-manager" ]; then
            echo "Root package name must be unified-ble-manager, got ${ROOT_NAME}"
            exit 1
          fi
          if [[ "$ROOT_VER" == 4.0.0-rc.* ]]; then
            echo "NPM_DIST_TAG=latest" >> "$GITHUB_ENV"
            echo "is_stable=false" >> "$GITHUB_OUTPUT"
          elif [[ "$ROOT_VER" == *-* ]]; then
            echo "NPM_DIST_TAG=next" >> "$GITHUB_ENV"
            echo "is_stable=false" >> "$GITHUB_OUTPUT"
          else
            echo "NPM_DIST_TAG=latest" >> "$GITHUB_ENV"
            echo "is_stable=true" >> "$GITHUB_OUTPUT"
          fi
          echo "Publishing ${ROOT_NAME}@${ROOT_VER} from tag ${GITHUB_REF_NAME}"

      # Check before enforcing current-main so an immutable npm publication can
      # finish its GitHub Release on a rerun even if main has advanced.
      - name: Check whether versions are already on npm
        id: npm_status
        run: |
          set -euo pipefail
          VER="$(node -p "require('./package.json').version")"
          PACKAGE_PUBLISHED=false
          if npm view "unified-ble-manager@${VER}" version >/dev/null 2>&1; then
            PACKAGE_PUBLISHED=true
            echo "unified-ble-manager@${VER} is already on npm"
          else
            echo "unified-ble-manager@${VER} is not on npm yet"
          fi
          echo "package_published=${PACKAGE_PUBLISHED}" >> "$GITHUB_OUTPUT"
          if [ "$PACKAGE_PUBLISHED" = true ]; then
            echo "already_published=true" >> "$GITHUB_OUTPUT"
            echo "Canonical package already on npm — will skip publish and continue to GitHub Release"
          else
            echo "already_published=false" >> "$GITHUB_OUTPUT"
          fi

      # Stable releases must be cut from the exact current main commit before
      # their initial npm publication. Once npm contains the immutable version,
      # reruns reuse that registry tarball solely to finish release creation.
      - name: Fetch main for stable tag verification
        if: steps.release_channel.outputs.is_stable == 'true' && steps.npm_status.outputs.package_published != 'true'
        run: git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main

      - name: Verify stable tag points at current main
        if: steps.release_channel.outputs.is_stable == 'true' && steps.npm_status.outputs.package_published != 'true'
        run: |
          set -euo pipefail
          TAG_COMMIT="$(git rev-parse "${GITHUB_REF_NAME}^{commit}")"
          MAIN_COMMIT="$(git rev-parse refs/remotes/origin/main)"
          if [ "${TAG_COMMIT}" != "${MAIN_COMMIT}" ]; then
            echo "Latest-channel tag ${GITHUB_REF_NAME} must point exactly at current main before initial publication"
            echo "tag:  ${TAG_COMMIT}"
            echo "main: ${MAIN_COMMIT}"
            exit 1
          fi

      # Do NOT set NODE_AUTH_TOKEN. npm exchanges the GitHub OIDC identity for
      # this publish command; a long-lived token would defeat that boundary.
      - name: Publish unified-ble-manager (OIDC + provenance)
        if: steps.npm_status.outputs.package_published != 'true'
        run: npm publish "${PUBLISH_TARBALL}" --provenance --access public --tag "${NPM_DIST_TAG}"

      - name: Bind npm tarball to the generated release artifact
        env:
          ALREADY_PUBLISHED: ${{ steps.npm_status.outputs.package_published }}
        run: |
          set -euo pipefail
          VER="$(node -p "require('./package.json').version")"
          REGISTRY_TARBALL=""
          for ATTEMPT in $(seq 1 36); do
            if REGISTRY_TARBALL="$(npm view "unified-ble-manager@${VER}" dist.tarball --json 2>/dev/null | node -e "let body = ''; process.stdin.setEncoding('utf8'); process.stdin.on('data', chunk => { body += chunk }); process.stdin.on('end', () => { const value = JSON.parse(body); if (typeof value !== 'string' || !value.startsWith('https://')) { throw new Error('npm dist.tarball must be an HTTPS URL'); } process.stdout.write(value) })")"; then
              break
            fi
            echo "npm registry metadata is not visible yet (attempt ${ATTEMPT}/36)"
            sleep 5
          done
          if [ -z "${REGISTRY_TARBALL}" ]; then
            echo "npm registry metadata did not become visible within the bounded retry window"
            exit 1
          fi

          REGISTRY_TARBALL_COPY="$(mktemp)"
          trap 'rm -f "${REGISTRY_TARBALL_COPY}"' EXIT
          curl --fail --location --silent --show-error --proto '=https' \
            --output "${REGISTRY_TARBALL_COPY}" \
            "${REGISTRY_TARBALL}"

          # A post-publish rerun must attach and hash the immutable npm bytes,
          # not a newly linked native build that may differ byte-for-byte.
          if [ "${ALREADY_PUBLISHED}" = true ]; then
            cp "${REGISTRY_TARBALL_COPY}" "${PUBLISH_TARBALL}"
            echo "Restored exact published npm tarball for release recovery"
          fi

          LOCAL_TARBALL_SHA256="$(node -e "const crypto = require('crypto'); const fs = require('fs'); process.stdout.write(crypto.createHash('sha256').update(fs.readFileSync(process.argv[1])).digest('hex'))" "${PUBLISH_TARBALL}")"
          REGISTRY_TARBALL_SHA256="$(node -e "const crypto = require('crypto'); const fs = require('fs'); process.stdout.write(crypto.createHash('sha256').update(fs.readFileSync(process.argv[1])).digest('hex'))" "${REGISTRY_TARBALL_COPY}")"
          if [ "${REGISTRY_TARBALL_SHA256}" != "${LOCAL_TARBALL_SHA256}" ]; then
            echo "Registry tarball SHA-256 does not match the exact generated publish tarball"
            exit 1
          fi

          PROVENANCE_VERIFIED=false
          for ATTEMPT in $(seq 1 36); do
            if npm view "unified-ble-manager@${VER}" dist.attestations --json 2>/dev/null | node -e "
              let body = '';
              process.stdin.setEncoding('utf8');
              process.stdin.on('data', chunk => { body += chunk });
              process.stdin.on('end', () => {
                const value = JSON.parse(body);
                if (typeof value?.url !== 'string' || !value.url.startsWith('https://registry.npmjs.org/')) {
                  throw new Error('npm attestation URL must use the npm registry HTTPS origin');
                }
                if (value?.provenance?.predicateType !== 'https://slsa.dev/provenance/v1') {
                  throw new Error('npm provenance predicate must be SLSA provenance v1');
                }
              });
            "; then
              PROVENANCE_VERIFIED=true
              break
            fi
            echo "npm provenance metadata is not visible yet (attempt ${ATTEMPT}/36)"
            sleep 5
          done
          if [ "${PROVENANCE_VERIFIED}" != true ]; then
            echo "npm provenance metadata did not become visible within the bounded retry window"
            exit 1
          fi

          {
            printf '%s  %s\n' "${LOCAL_TARBALL_SHA256}" "${PUBLISH_TARBALL}"
            shasum -a 256 SBOM.cdx.json THIRD_PARTY_LICENSES.json
          } > SHA256SUMS
          shasum -a 256 --check SHA256SUMS

      - name: Bind npm provenance to this exact tag commit
        run: |
          set -euo pipefail
          VER="$(node -p "require('./package.json').version")"
          ATTESTATIONS_FILE="$(mktemp)"
          trap 'rm -f "${ATTESTATIONS_FILE}"' EXIT
          ATTESTATIONS_READY=false
          for ATTEMPT in $(seq 1 36); do
            if ATTESTATIONS_URL="$(npm view "unified-ble-manager@${VER}" dist.attestations.url --json 2>/dev/null | node -e "let body = ''; process.stdin.setEncoding('utf8'); process.stdin.on('data', chunk => { body += chunk }); process.stdin.on('end', () => { const value = JSON.parse(body); if (typeof value !== 'string' || !value.startsWith('https://registry.npmjs.org/')) throw new Error('npm attestation URL must use the npm registry HTTPS origin'); process.stdout.write(value) })")" && \
              curl --fail --location --silent --show-error --proto '=https' \
                --output "${ATTESTATIONS_FILE}" \
                "${ATTESTATIONS_URL}" && \
              test -s "${ATTESTATIONS_FILE}"; then
              ATTESTATIONS_READY=true
              break
            fi
            echo "npm provenance attestations are not visible yet (attempt ${ATTEMPT}/36)"
            sleep 5
          done
          if [ "${ATTESTATIONS_READY}" != true ]; then
            echo "npm provenance attestations did not become visible within the bounded retry window"
            exit 1
          fi
          node scripts/ci/verify-npm-provenance-source.js \
            "${ATTESTATIONS_FILE}" \
            "${GITHUB_SHA}" \
            "${GITHUB_REF_NAME}" \
            "https://github.com/sfourdrinier/unified-ble-manager"

      # Tags remain manual. After npm succeeds, create the GitHub Release from
      # CHANGELOG.md notes so the Releases page matches the published package.
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME}"
          VER="${TAG#v}"

          if gh release view "${TAG}" >/dev/null 2>&1; then
            echo "GitHub Release ${TAG} already exists — skipping"
            exit 0
          fi

          NOTES_FILE="$(mktemp)"
          {
            echo "## ${TAG}"
            echo
            BODY="$(awk -v ver="${VER}" '
              $0 ~ ("^## \\[" ver "\\]") { p = 1; next }
              p && $0 ~ /^## \[/ { exit }
              p { print }
            ' CHANGELOG.md)"
            if [ -n "${BODY}" ]; then
              printf '%s\n' "${BODY}"
            else
              echo "See CHANGELOG.md for ${TAG} details."
            fi
            echo
            echo "### npm package (published with provenance)"
            echo
            echo "- \`unified-ble-manager@${VER}\` — canonical product"
            echo "  - tarball: https://registry.npmjs.org/unified-ble-manager/-/unified-ble-manager-${VER}.tgz"
          } > "${NOTES_FILE}"

          if [[ "${VER}" == *-* ]]; then
            gh release create "${TAG}" \
              --title "${TAG}" \
              --notes-file "${NOTES_FILE}" \
              "${PUBLISH_TARBALL}" \
              SBOM.cdx.json \
              THIRD_PARTY_LICENSES.json \
              SHA256SUMS \
              --prerelease \
              --verify-tag
          else
            gh release create "${TAG}" \
              --title "${TAG}" \
              --notes-file "${NOTES_FILE}" \
              "${PUBLISH_TARBALL}" \
              SBOM.cdx.json \
              THIRD_PARTY_LICENSES.json \
              SHA256SUMS \
              --verify-tag
          fi

          echo "Created GitHub Release ${TAG}"
          rm -f "${NOTES_FILE}"
