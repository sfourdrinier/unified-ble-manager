# API Report — unified-ble-manager/expo

```ts
export function createExpoBleManager(options?: BleManagerCreateOptions): Promise<BleManager>
export function createExpoBleManagerWithEnvironment(environment: unknown, options?: BleManagerCreateOptions): Promise<BleManager>
```
