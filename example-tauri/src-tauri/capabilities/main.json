{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "BLE access for the proving consumer's main window only.",
  "windows": ["main"],
  "permissions": ["core:default", "unified-ble-manager:default"]
}
